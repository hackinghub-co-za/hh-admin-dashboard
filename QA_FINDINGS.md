# QA Findings

A running QA log for hh-admin-dashboard, produced by the `qa-agent` (`.claude/agents/qa-agent.md`). The app is too large to QA in one pass — each invocation claims one or two unchecked areas below, works them to completion, fixes clear-cut bugs directly (committed to `main`), and logs everything else as a finding for a human call.

Started 2026-09-11.

## Coverage checklist

### Member-facing
- [x] Dashboard (Journey So Far, Getting Started checklist, login streak, survey banner, next 1-on-1, recommended room, community broadcasts, recent wins, suggested content)
- [x] My Roadmap (Core Foundations / Specialization / Projects / Advanced phases, unlock gates, progress logging, LinkedIn weekly-post plan)
- [x] Matchmaker (opt-in, group formation, wheel reveal, presentation ratings/showcase)
- [x] Members directory (search, grouped-by-domain view, profile modal, referrals, Security/passkeys panel)
- [x] 1-on-1 Meetings (booking, CV Review, Interview Prep, other Gemma tools)
- [x] Events (RSVP, capacity, images, self-submission flow, past-event filtering)
- [x] Job Board (member view)
- [x] Resources (Cert Prep guides, LinkedIn Strategy, Podcasts)
- [x] Breakdowns (archive list, markdown rendering, unsubscribe)
- [x] Cert Calendar (booking, Exam Readiness tracker, practice quizzes)
- [x] Competitions (daily room logs, standings + tie-splitting, Head-to-Head Duels, Room Races, Live Buzzer Trivia, rules guide)
- [x] Reviews (member view)
- [x] My Subscription & Upgrades (PayFast checkout, EFT details, merch store + orders)
- [x] Onboarding sequence + Getting Started hard gate (3-day grace)
- [x] Offboarding sequence (member marked Leaving, exit feedback)
- [x] Passkey sign-in + Security panel (member and staff sides, Login.jsx)

### Admin/staff-facing
- [x] Admin Overview (metrics, churn rate, revenue per member)
- [x] Members management (roster, status transitions, role assignment gating)
- [x] Roadmaps management (approvals, catalog assignment, Projects proof review)
- [x] Room Logs review (approve/reject, standings updates)
- [x] Meetups & Events admin (approve/reject, image upload, capacity, add-event auto-approve)
- [x] Job Board admin
- [x] Merch Orders
- [x] Payments & Subscriptions
- [x] Cert Calendar admin (result marking, cert-pass email trigger)
- [x] Finances
- [x] Reviews admin
- [x] Insights
- [x] Community Content (broadcasts, wins, suggested content, Weekly Breakdowns management)
- [x] Team & Roles (role assignment, permission-scope correctness per role)

### Cross-cutting / backend
- [x] Email edge functions (roadmap-reminder, linkedin-post-reminder, cert-pass, matchmaker-group, weekly-breakdown-email/nudge/unsubscribe)
- [x] Push notification edge functions (1on1 reminder, new job, streak alert)
- [x] Gemma AI edge functions (chat, CV/LinkedIn review, interview prep)
- [x] Cron jobs (schedules correct, CRON_SECRET checked, each job idempotent if it fires twice)
- [x] RLS/permission-scope audit across `067_permission_scopes.sql` (does every widened table/RPC actually match the intended role matrix?)
- [x] Referral program (Refer a Friend, reward status transitions)

## Findings log

_(newest first)_

### 2026-09-16

Every coverage-checklist box was already checked off, and the newest batch of work (everything since the 2026-09-13 entry below) was the newest/least-battle-tested code, so per that entry's own precedent this pass did a focused re-check of that batch instead of the general checklist: `cc1a04e` through `2e80678` (Cert Calendar member self-edit, Job Board track tagging + the job-recommendation-email/unsubscribe Edge Functions, TryHackMe recoverable prize-eligibility checkpoints, Study Hours Phase 1 including its later track→cert rename, the live `cert_calendar`/`job_board`/`community_broadcasts` id-sequence regression and its fix, and the new Soft Skills Resources category). Read every diff in the batch plus `024_cert_calendar.sql`, `025_job_board.sql`, `044_community_content.sql`, `070_competition_seasons.sql`, `081_study_sessions.sql`, `026_resources.sql`, `src/lib/certCalendarData.js`/`jobBoardData.js`/`competitionData.js`/`studyHoursData.js`, `supabase/functions/job-recommendation-email/index.ts` and its `-unsubscribe` sibling, and the relevant Cert Calendar/Job Board/Competitions/Resources slices of `MemberPortal.jsx` and `AdminDashboard.jsx`. Grepped every `setval()` call in the whole `supabase/` directory (not just the three already-fixed files) for the same unguarded-rewind pattern, and read-only-verified via `npx supabase db query --linked` that `cert_calendar`/`job_board`/`community_broadcasts`'s live sequences currently sit exactly at their real max id (no lag). `npm run build && npm run lint` held at baseline (36 problems: 35 errors, 1 warning) both before and after - no code changes made this pass, see below.

This batch read exceptionally clean - the most thorough, careful set of commits QA'd so far. Specifically verified and found correct:
- `update_my_cert_calendar_entry()` (`024_cert_calendar.sql`): server-side ownership check by JWT email, only ever touches `cert_name`/`date`/`cohort` - `member`/`member_email`/`created_by`/`result` are unreachable through it, exactly matching the commit's own stated intent (self-marking your own exam result would defeat the point of admin verification). Same `GRANT ... TO authenticated` / `REVOKE ... FROM PUBLIC, anon` idiom as every other self-service RPC in this codebase.
- `setval()` audit: every single `setval()` call left in `supabase/` (7 total, across `019_events.sql`, `024_cert_calendar.sql`, `025_job_board.sql`, `026_resources.sql`, `044_community_content.sql` ×2) is now `GREATEST`-guarded against the table's real current max id - no other instance of the bare-hardcoded-value pattern that caused the live `cert_calendar`/`job_board`/`community_broadcasts` collision exists anywhere in the migrations directory. Live sequence check confirmed all three fixed tables currently healthy (`cert_calendar`: max/seq both 36, `job_board`: both 10, `community_broadcasts`: both 3).
- `job-recommendation-email`: the Interview Ready + track-match filter (`member_profiles.roadmap_track = job.track AND job_readiness = 'Interview Ready' AND job_recommendation_opted_out = false AND status != 'Left'`) matches real column names/values (`JOB_READINESS_STAGES` in `memberOptions.js` does contain the literal string `'Interview Ready'`), the job's own `track` is re-read server-side from the DB by `job_id` rather than trusted from the client, a trackless listing is correctly skipped, and the poster themselves is excluded from their own notification. The unsubscribe function/edge function pair follows the identical anonymous-`p_email`, `--no-verify-jwt` pattern as `breakdown-unsubscribe`/`unsubscribe_from_linkedin_reminders`.
- Study Hours' track→cert rename (`8308308`) left no stale references anywhere - `studyHoursData.js`'s `logStudySession` correctly sends `p_cert`, the RPC signature was properly dropped-and-recreated (not just `CREATE OR REPLACE`d, which can't rename a parameter) with a guarded `DO $$ ... RENAME COLUMN track TO cert ... $$` for a database that ran the file before the rename, and the Pomodoro UI's cert-picker (`MemberPortal.jsx`) fully replaced the old `ROADMAP_TRACKS` dropdown with the same `CERT_CATALOG_BY_VENDOR` two-step picker Cert Calendar uses - no leftover `studyTrack` variable or `ROADMAP_TRACKS` import in that section. `study_leaderboard`/`study_sessions` RLS and RPC grants (`join_study_hours`/`opt_out_of_study_hours`/`log_study_session`) match the established "self-service via SECURITY DEFINER RPC, no direct table write policy, name resolved server-side never client-trusted" pattern `rsvp_for_competition` set. `planned_minutes` is re-validated server-side (`CHECK (planned_minutes IN (25, 45, 60))` at the table level, and again in the RPC body) even though the client only ever offers those three options in the UI.
- TryHackMe prize-eligibility checkpoints (`792062c`): `get_competition_prize_eligibility()` computes live against `daily_room_logs` (`status = 'Approved'`, `log_date >= start_date`), fails open (everyone eligible) both at the SQL level (no checkpoint reached yet → `v_required = 0`) and at the client level (`competitionData.js`'s try/catch defaults every row to `eligible ?? true` on any fetch error) - a real bug in this RPC could never wrongly disqualify a genuine prize winner, only wrongly include one, which is the safer failure direction for real prize money. Confirmed the leaderboard *rank* number is deliberately unaffected by eligibility (an ineligible pace-behind leader keeps their real rank/row position, just loses the medal/prize highlight) - matches the feature's own "never loses their leaderboard spot" design goal, not a bug.
- Soft Skills Playlist (`2e80678`): `Handshake` icon was already imported in `MemberPortal.jsx` (reused from the Matchmaker section) so no missing-import build break; the six external links (4 TED talks, 2 Indeed Career Guide articles) in `SoftSkillsGuideModal.jsx` are all real `https://` URLs, not placeholders; the `resources_category_check` CHECK constraint was correctly widened via `DROP CONSTRAINT IF EXISTS` + re-`ADD CONSTRAINT` (the "existing table" ALTER path, not just the inline `CREATE TABLE` CHECK which is a no-op on a live DB) - the same idiom the file's own prior category additions (`Cyber Platforms`, `LinkedIn Strategy`) already used correctly.

#### [Docs] CHANGELOG.md was never updated for the 2026.09.14 or 2026.09.15 batches
- **Severity**: Low (documentation-only, no behavioral impact)
- **Where**: `CHANGELOG.md` (top of file still reads `## 2026.09.13`); `src/data/releaseNotes.js` (top entry is `2026.09.14`, added by `e7a55cb`/`8d301f0` on 2026-09-14)
- **What's wrong**: `src/data/releaseNotes.js` has a member-facing entry for the whole 2026.09.14 batch (Cert Calendar self-edit, Job Board track tags + recommendation email, TryHackMe prize-eligibility checkpoints, Study Hours), but `CHANGELOG.md` - the technical/admin half of the same versioning strategy the file's own header describes - has no matching `## 2026.09.14` section at all, and neither file has anything for the 2026.09.15 batch (the live `cert_calendar`/`job_board`/`community_broadcasts` sequence-collision incident and its root-cause fix, and the new Soft Skills Playlist resource, which IS member-facing and would normally get a `releaseNotes.js` entry the same way Study Hours/Cert Calendar self-edit did). This isn't a behavioral bug and doesn't affect any live user, but it is a real gap against this project's own stated documentation discipline (every prior batch got a same-week "docs: release notes for the ... batch" / "docs: catch up ..." commit - see `193ce28`, `00175b5`, `e7a55cb`, `8d301f0` - and this is the first batch since 2026-09-11 where that follow-up commit never happened).
- **Is this intentional?** Almost certainly just not-yet-done rather than deliberate - matches the exact shape of the 2026-09-13 pass's own "catch up release notes/CHANGELOG" commit (`00175b5`), which was a routine follow-up, not a policy change. Not fixed directly this pass: writing the CHANGELOG's `Added`/`Changed`/`Fixed` technical summary and the release notes' plain-language member copy for a ~15-commit batch is exactly the kind of curated, voice-matched writing this project's own header comment calls out ("keep it in plain, benefit-focused language") - worth doing deliberately in one pass by whoever next has full context on the batch, not guessed at by QA.
- **Status**: Logged, not yet fixed.

**Next up**: The coverage checklist has no unclaimed boxes left, and this batch (`cc1a04e` through `2e80678`) is now the most recently re-checked slice - whoever picks this up next has three reasonable options: (1) the CHANGELOG/release-notes catch-up finding just above, if it hasn't been done by then; (2) another narrow "re-check the newest commits since this entry" sweep once more work has landed; or (3) a second, deeper pass over an area with the most real bugs found historically (Finances/Admin Overview's revenue-metric drift, or the Cert Calendar/Competitions name-spoofing family) in case a similar pattern was reintroduced. Still open from earlier passes, unchanged: the Cert Calendar display-name spoofing question (needs a founder product decision, logged 2026-09-13), the mentor-UI gap, the quarterly-competition-reset firing (deliberately not yet triggered), the "Tegranyota2004" orphan-refund data anomaly, and the broader push-notification feature's genuine incompleteness.

### 2026-09-13

Every coverage-checklist box was already checked off by the 2026-09-12 ninth pass. Per this pass's own instructions, claimed a focused re-check of the newest, least-battle-tested work instead: the whole 2026.09.13 commit batch (`193ce28` back through `7fc4a54`) - Member Sheet filters/Tier column/roadmap completion %, the CISCO Cybersecurity Defense Analyst course addition, vendor logos on Resources/My Roadmap, the Domain Info Modal, equal-height Member Directory cards, the Elite Operative apply-link fix, the `matchmaker-group-email` CORS fix, the TryHackMe weekday cap change, and the `community_wins` id-sequence fix. Read every diff in the batch, `supabase/031_daily_room_logs.sql`, `supabase/026_resources.sql`, `supabase/028_roadmap.sql`, `src/lib/memberOptions.js`'s new `TRACK_INFO`/`VENDOR_LOGO_*` tables, `src/components/DomainInfoModal.jsx`/`GroupedMemberDirectory.jsx`, and cross-checked `src/data/releaseNotes.js`/`CHANGELOG.md`'s claims about this batch against the actual live database (read-only queries via `npx supabase db query --linked`, no writes to any real member row). `npm run build && npm run lint` confirmed at baseline (36 errors, 1 warning) both before and after.

#### [Resources / My Roadmap] CHANGELOG/release notes claimed the CISCO Cybersecurity Defense Analyst course was "added to Resources... including a backfill onto every existing SOC-track member's roadmap" - neither addition existed in any committed migration file
- **Severity**: Low (docs-vs-migration-files mismatch, not a live user-facing bug - see below)
- **Where**: `src/data/releaseNotes.js` / `CHANGELOG.md` (2026.09.13 entries) vs. `supabase/026_resources.sql` / `supabase/028_roadmap.sql` (pre-fix)
- **What's wrong**: The new course was added to `SPECIALIZATION_CATALOGS.SOC` in `memberOptions.js` (governs the admin's manual, one-member-at-a-time "Add Standard Specialization" quick-fill and new-member catalog assignment), but grepping the entire `supabase/` tree found zero mention of "Defense Analyst" anywhere - no INSERT into `resources`, no backfill of `roadmap_items` for existing SOC members. Read-only live queries confirmed both actions had, in fact, already been done live by hand (a `resources` row, id 17, "Cisco Cybersecurity Defense Analyst"; and a `roadmap_items` row for every SOC-track member who already had the other 4 catalog items) - so no live member is currently missing either the Resources entry or their roadmap item. But since neither action was ever captured in a migration file, a database rebuilt from `supabase/0NN_*.sql` alone (disaster recovery, a fresh dev/staging environment) would silently regenerate without either - a real, if narrow, drift-from-docs risk, and the CHANGELOG's claim of "including a backfill" was, at the time it was written, not actually backed by anything in the repository history.
- **Status**: Fixed - added a guarded, idempotent INSERT to `026_resources.sql` (title matched exactly to the live row's existing casing, "Cisco Cybersecurity Defense Analyst", to avoid creating a case-sensitive duplicate of what already exists) and a guarded backfill to `028_roadmap.sql` (every SOC-track member who already has the other 4 SOC Specialization items and doesn't yet have this one gets it added, mirroring the admin quick-fill button's own logic). Applied both live via `npx supabase db query --linked --file`; verified both are true no-ops against current data (no duplicate resource row, `soc_items`/`has_new` counts unchanged for every SOC member) - this is purely a "make the migration files match what already happened live" fix, with zero live behavior change today, but now safe for a fresh install or DR rebuild.

Everything else read in this pass held up cleanly, no further bugs found:
- **Member Sheet filters/Tier/roadmap %**: `meetingStatusFor()` (new filter helper) uses the exact same `daysSince > MEETING_OVERDUE_AFTER_DAYS` formula as the per-row `meetingOverdue` computed a few lines below it - no drift between the filter and the displayed badge. `roadmapPercentFor()` correctly derives from `allRoadmapItems` (already-loaded roster-wide data, keyed by `item.memberEmail`, matching `roadmapData.js`'s own field naming) rather than a second manual field, so it can't drift from the real per-member roadmap state the way a duplicated computation elsewhere in this app already has (see the Admin Overview/Finances revenue-metric finding from 2026-09-12). Tier filter's dropdown is correctly built from `sheetDistinctTiers` (only tiers actually present among active members), while Specialty's dropdown uses the full static `SPECIALTIES` list regardless of what's present - a minor, harmless UX asymmetry (picking an absent specialty just yields zero rows, no wrong data), not a bug.
- **CISCO course / vendor logos**: `vendorLogoFor()`'s `\bcisco\b` pattern correctly matches both "CISCO Junior Cyber Pathway" and "CISCO Cybersecurity Defense Analyst" (whole-word, case-insensitive). The Microsoft four-color-grid fix (`c1e7691`, adding `gridTemplateRows` alongside the existing `gridTemplateColumns`) is a correct, narrowly-scoped CSS fix - content-less `<span>`s genuinely have no intrinsic height for `auto` grid rows to size against without it.
- **Domain Info Modal**: `TRACK_INFO` covers all 7 real tracks in `ROADMAP_TRACKS`/`TRACK_DESCRIPTIONS` with no gaps; the domain name's `onClick` correctly calls `e.stopPropagation()` so opening the modal doesn't also toggle the parent group card's own expand/collapse.
- **Equal-height Member Directory cards**: `ExpandableText`'s toggle button also correctly calls `e.stopPropagation()` (added this batch) so clicking "Read more" inside a directory card doesn't also fire the card's own onClick-opens-profile-modal handler.
- **TryHackMe weekday cap**: the CHECK constraint (table-level, `NOT VALID` so historical rows logged under the old 1-5 rule aren't retroactively invalidated) and `submit_daily_room_log()`'s own pre-check are both correctly updated to 3/2 and stay in sync with each other; `MemberPortal.jsx`'s `maxRoomsToday` dropdown cap matches both.
- **`matchmaker-group-email` CORS fix**: matches the exact `corsHeaders`/OPTIONS-preflight pattern already used by every other browser-invoked edge function in this project (`gemma-review`, `cert-pass-email`, etc.) - every response path, including every error branch, now carries the header.
- **`community_wins` id-sequence fix**: identical shape and fix to the already-known `community_events` sequence bug - `GREATEST()` against the real live `max(id)` instead of a bare literal.
- **Elite Operative "Apply for Placement" link fix**: a plain URL swap, nothing else touches that code path.

**Next up**: Nothing else claimed this pass - this was a deliberately narrow, single-batch re-check rather than a new checklist area (the whole checklist was already closed out as of 2026-09-12's ninth pass). Whoever picks this up next has two good options: (1) keep doing these narrow "re-check today's newest commits" sweeps as fresh batches land, since the coverage checklist itself offers no more unclaimed boxes; or (2) do a second, deeper pass over an already-checked area that's had the most real bugs found in it so far (Finances/Admin Overview's revenue-metric drift, or the Cert Calendar/Competitions name-spoofing family) in case a similar pattern was reintroduced by later work. Still open from earlier passes, unchanged: the mentor-UI gap (real feature build, not a bug), the quarterly-competition-reset firing (deliberately not yet triggered), the "Tegranyota2004" orphan-refund data anomaly (needs a manual look), the Cert Calendar display-name spoofing question (needs a founder product decision), and the broader push-notification feature's genuine incompleteness (missing Firebase secrets/tables).

---

### 2026-09-12 (second pass, same day)

Claimed: **Admin Overview** (metrics, churn rate, revenue per member) + **Finances** - both revenue-calculation-heavy areas flagged by prior passes as good "complex logic / money-handling" picks, and both render off the same `payments` array (live PayFast + EFT + historical), so read together naturally. Read `payfastPaymentsData.js`, `memberData.js`, the whole `payments`/`memberRosterMap`/metrics block in `AdminDashboard.jsx` (~line 2290-2420), the Admin Overview render (~line 2722-2830), and the Finances tab render (~line 6593-6930). Both checklist boxes checked off.

#### [Finances / Members management] Refunded PayFast payments weren't reducing a member's Total Spent on HH
- **Severity**: High
- **Where**: `src/views/Admin/AdminDashboard.jsx` - `memberRosterMap`'s build filter (~line 2409, pre-fix)
- **What's wrong**: The live `payfast_transactions` table marks a refund `payment_status = 'REFUNDED'`, and `payfastPaymentsData.js` maps that straight through as the row's `type` (`type: row.payment_status === 'COMPLETE' ? 'Funds Received' : row.payment_status`). But `memberRosterMap`'s filter only ever recognized `'Funds Received'` or `'Funds Received (Reversal)'` - the second string is the *historical CSV import's* own convention for the same concept (baked into `payfastTransactions.mock.json`), never what the live table actually produces. A real refund's negative `amount` matched neither string, so it was invisible to `entry.totalSpent += p.amount` entirely - a refunded member's Total Spent stayed at their pre-refund total forever, on every screen that reads it (Members tab card, Member Sheet, `MemberProfileModal`'s "Total Spent on HH"). Confirmed live via a read-only query: `sashamartinndau@gmail.com` paid R200 + R300 (both COMPLETE), then got the R300 refunded on 2026-03-31 (`REFUNDED`, `amount: -300`) - every "Total Spent" display for her showed R500, not the real R200, and would keep doing so indefinitely since nothing about this bug is time-limited.
- **Status**: Fixed (commit `e6233c4`) - the filter now also matches `'REFUNDED'`, so a live refund correctly reduces `totalSpent` the same way the code's own pre-existing (but non-functional-in-practice) reversal-handling intent already implied. Verified: Sasha's totalSpent now correctly computes to R200.

#### [Admin Overview / Finances] Total Gross Sales, Net Payout, Fees Paid, Total Transactions, and Monthly Recurring Revenue summed the *entire* `payments` array with no type filter, unlike every sibling metric on the same tabs
- **Severity**: Medium-High
- **Where**: `src/views/Admin/AdminDashboard.jsx` - `totalGrossRevenue`/`totalFeesPaid`/`totalNetRevenue`/`totalTransactions`/`monthlyRecurringRevenue` (~line 2321-2336, pre-fix), plus their exact duplicates `yearlyRevenue`/`monthlyRevenue`/`weeklyRevenue`/`planBreakdown` re-derived independently inside the `'finances'` case (~line 6598-6657, pre-fix)
- **What's wrong**: Every other revenue-related figure on these two tabs (`emailsPaidBetween`, `allTimeMemberEmails`, `paymentCountsByEmail`, the `revenueTrend` chart, `upcomingPayments`) correctly filters to `p.type === 'Funds Received'` before summing. These nine values (five on Admin Overview/Payments, four duplicated again on Finances) didn't filter by `type` at all - they summed literally every row in `payments`, so a `REFUNDED` row's negative amount silently leaked into some numbers on the page and not others, a classic "same value computed two different ways, could drift" bug. Live impact today: both real `REFUNDED` rows (-R300, -R750) were already dragging down `totalGrossRevenue`/`yearlyRevenue`/etc. by the correct net amount, by accident, while `totalFeesPaid` was simultaneously *increased* by those two refunds' original fee amounts (R11.04 + R27.60) since a refund's `fee` column still has its original positive value and nothing distinguished "fee paid on a completed sale" from "fee that was on a since-refunded sale." Also a forward-looking risk: `payfastPaymentsData.js`'s own mapping can produce `type: 'PENDING'`/`'FAILED'`/`'CANCELLED'` for a real PayFast ITN that never actually completed - none exist in the live table today, but the moment one does, these nine figures would have silently counted money that never landed as real revenue.
- **Status**: Fixed (commits `e6233c4`, `0183f19`) - introduced one shared `isRevenueRow`/`revenuePayments` filter (`type === 'Funds Received' || type === 'REFUNDED'`) and pointed all nine calculations at it, matching the already-correct sibling metrics exactly. Numerically identical to before for `totalGrossRevenue`/`totalFeesPaid`/`totalNetRevenue`/`totalTransactions`/`yearlyRevenue`/`monthlyRevenue`/`weeklyRevenue`/`planBreakdown` today (no PENDING/FAILED/CANCELLED rows exist yet), so this is both a consistency fix and a guard against a real live discrepancy the moment one does.

#### [Finances] "Tegranyota2004" has a REFUNDED payment (-R750) with no matching original completed payment anywhere in `payfast_transactions` for that email
- **Severity**: Low (data integrity, not a code bug)
- **Where**: Live `payfast_transactions` data - `tegranyota2004@gmail.com`, `pf_payment_id 277495459`
- **What's wrong**: Every other refund in this table has a clear prior COMPLETE payment it's reversing. This one doesn't - the only row on file for this email is the refund itself. This isn't something a code fix can resolve (there's no logic bug here to correct; either the original charge was recorded under a different email/never made it into this table during a historical migration, or this is a genuine one-off data gap). Consequence, post the fix above: this member now shows up in the roster for the first time (previously invisible, since they had no `'Funds Received'`-type row at all to seed a `memberRosterMap` entry) with a negative Total Spent (-R750) - technically more correct than being silently absent, but a negative currency figure on a member card reads oddly and is worth a human glance.
- **Status**: Logged, needs a manual data check - not something to guess-fix by inserting a fabricated original-payment row for a real member's real financial history.

**Next up**: **Members management** (roster, status transitions, role assignment gating) is the natural continuation - it shares the exact same `memberRosterMap`/`memberProfiles` data this pass just fixed, and hasn't been read yet. Also still unclaimed: **Meetups & Events admin**, **Job Board admin**, **Reviews (member + admin)**, **Community Content**, **Team & Roles**, **Dashboard (member home tab)**, **Members directory**, **1-on-1 Meetings**, **Resources**, **Breakdowns**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (push notification edge functions, Gemma AI edge functions, cron jobs, referral program).

---

### 2026-09-12 (third pass, same day)

Claimed: **Members management** (roster, status transitions, role assignment gating) - the natural continuation flagged by the pass just above, since it shares the exact `memberRosterMap` this pass had just fixed. Read `004_member_access_control.sql` (`is_member_allowed`, `grant_member_portal_access`), `memberData.js`, `MemberProfileModal.jsx`'s status-transition logic (`updateStatus`, `isLapsed`), `memberOptions.js`'s `MEMBERSHIP_STATUSES`, and re-read `memberRosterMap`'s build in `AdminDashboard.jsx` after this morning's own fixes to it.

#### [Members management] A member's real payment silently un-set "Active (Permanent)" back to plain "Active"
- **Severity**: High
- **Where**: `supabase/004_member_access_control.sql` - `grant_member_portal_access()` (pre-fix)
- **What's wrong**: This function runs after *every* payment - PayFast (via `payfast-webhook`, service role) and every admin-recorded EFT (`handleRecordEftPayment` → `grantMemberPortalAccess()`) - and its `ON CONFLICT` clause unconditionally set `status = 'Active'`, with no check of what the member's status already was. `'Active (Permanent)'` exists specifically so a member who's paid in full and owes no more recurring dues stops being flagged as lapsed (`MemberProfileModal`'s own copy: "If they've paid in full or don't owe recurring dues (e.g. Permanent Access), set this to 'Active (Permanent)' so it stops flagging"). But the very next payment logged for that member - an upgrade, a correction, a manually-recorded EFT top-up like the ones recorded for real Permanent Access members earlier this same session - would silently reset them back to plain `'Active'`, undoing that admin's explicit choice with no warning anywhere. From then on, `isLapsed` (`form.status === 'Active' && daysSinceLastPayment > LAPSED_AFTER_DAYS`) would eventually start flagging them as lapsed again the moment enough days passed without another payment - exactly the false-positive the status exists to prevent, reintroduced by the one action (a payment) that should least cause it. Verified live via a disposable test row: seeded `'Active (Permanent)'`, called `grant_member_portal_access()` once, came back `'Active'`.
- **Status**: Fixed (commit `94f6cb6`) - `status` is now only reset to `'Active'` when it isn't already `'Active (Permanent)'` (a `CASE` in the `ON CONFLICT` clause); `'Left'`/`'Leaving'` still correctly reactivate to `'Active'` on a new payment, unchanged. Applied live via `npx supabase db query --linked`; re-verified with the same disposable test row (seeded `'Active (Permanent)'` → call → stays `'Active (Permanent)'`; seeded `'Left'` → call → correctly becomes `'Active'`), then deleted the test row. No way to detect *retroactively* who this already silently downgraded before today, since there's no audit trail on `member_profiles.status` - this only stops it from happening going forward. 15 members currently carry `'Active (Permanent)'` live today.

Everything else read in this pass held up: `is_member_allowed()`'s allow-list logic (`status != 'Left'` correctly lets `'Leaving'` still sign in for the farewell screen, matching its own documented one-more-sign-in behavior); the admin-only gating on `grant_member_portal_access()`/`permanentlyDeleteMember` (RLS-enforced via `is_admin()`, consistent with the client-side "only rendered for a member already marked 'Left'" delete-button gate); `updateStatus()`'s one-time `offboardingStartedAt` stamp (correctly doesn't reset itself on every keystroke while already `'Leaving'`).

**Next up**: Still unclaimed - **Meetups & Events admin**, **Job Board admin**, **Reviews (member + admin)**, **Community Content**, **Team & Roles**, **Dashboard (member home tab)**, **Members directory**, **1-on-1 Meetings**, **Resources**, **Breakdowns**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (push notification edge functions, Gemma AI edge functions, cron jobs, referral program).

---

### 2026-09-12 (fourth pass, same day)

Claimed: **Events** (member: RSVP, capacity, images, self-submission, past-event filtering) + **Meetups & Events admin** (approve/reject, image upload, capacity, add-event auto-approve) - a natural pair, both checklist items unclaimed. Read `019_events.sql` (the consolidated, current source of truth per its own header - approval is further restricted by `067_permission_scopes.sql`, already covered by the earlier permission-scope pass), `src/lib/eventsData.js`, and the Events tab in both `MemberPortal.jsx` and `AdminDashboard.jsx` (case `'events'` / case `'meetups'`). Both checklist boxes checked off.

#### [Meetups & Events admin] "Live Events" list never filtered out past events, despite its own copy claiming it does
- **Severity**: Medium
- **Where**: `src/views/Admin/AdminDashboard.jsx` - `liveCommunityEvents` (~line 2094, pre-fix)
- **What's wrong**: The member-side Events tab already has a dedicated, previously-shipped fix filtering to upcoming-only (`daysUntilEvent(e.date) >= 0` in `MemberPortal.jsx`, referenced in this file's own recent git history: "fix: hide past events from the member Events tab"). This admin-side list's own header comment claimed it shows "the exact same data the member-side Events tab shows," and its own empty-state copy already read "Nothing approved and upcoming yet" - but the actual filter was just `e.status === 'Approved'`, with no date check at all. Confirmed live: 7 real approved events already in the past (`Meet-up` 2026-08-13, two `SC-900 Study Session`s, `0xCoffee JHB`, `HH Catchup`, `Sunday Catchup`, `Security Tech Update` 2026-09-10) were sitting in this list today, and would keep accumulating forever as more events pass, contradicting both its own documentation and its own displayed copy.
- **Status**: Fixed (commit `dabd123`) - added the same "event date >= start of today" filter, computed with a fresh `Date()` rather than reusing this function's `today` variable (which is declared later in the same function body - referencing it here would throw a temporal-dead-zone error). Note: this does mean there's now no UI path to edit a past-but-approved event's details (e.g., fix a typo after the fact) - previously possible since it just sat in the same undifferentiated list forever. Matches the same tradeoff the founder already accepted on the member side; not treated as a regression worth blocking on, but worth knowing about if a "past events archive" view is ever wanted.

Everything else read in this pass held up: `rsvp_for_event()`'s capacity enforcement (server-side, correctly lets an already-RSVP'd member back in regardless of a full event, since they're not taking a new seat); `unrsvp_from_event()`'s idempotent no-op shape; the member-side "Add Event" always landing Pending server-side (`WITH CHECK status = 'Pending'`, so a crafted request can't insert a pre-approved event); the admin "Add Event" form's auto-approve-if-eligible logic (`canApproveEvents`, matching `approve_community_event()`'s own siya-or-community_manager gate exactly - already verified correct in the earlier permission-scope pass); `event-images` storage bucket policies (public read, staff-only write); the HH-branding auto-logo assignment/clearing logic when switching event type; `uploadEventImage()`'s one-file-per-event-id replace-on-reupload behavior.

**Next up**: Still unclaimed - **Job Board (member view + admin)** (a natural next pair, same shape as this pass), **Reviews (member + admin)**, **Community Content**, **Team & Roles**, **Dashboard (member home tab)**, **Members directory**, **1-on-1 Meetings**, **Resources**, **Breakdowns**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (push notification edge functions, Gemma AI edge functions, cron jobs, referral program).

---

### 2026-09-12 (fifth pass, same day)

Claimed: **Job Board** (member + admin) and **Reviews** (member + admin) - two smaller, structurally similar areas (self-service member submission + simple admin CRUD, no moderation step on either), read together since both were quick. Read `025_job_board.sql`, `005_reviews.sql`, `src/lib/jobBoardData.js`, `src/lib/reviewsData.js`, and the Job Board / Reviews tabs in both `MemberPortal.jsx` and `AdminDashboard.jsx`. All four checklist boxes checked off.

#### [Reviews] RLS policies never lowercased the caller's JWT email before comparing - the one table in this codebase still doing the case-sensitive version of this check
- **Severity**: Medium
- **Where**: `supabase/005_reviews.sql` - `"Members insert own reviews"` and `"Members view public and own reviews"` (pre-fix)
- **What's wrong**: `submitReview()` (`reviewsData.js`) already lowercases `email` before every INSERT (`email: email.toLowerCase()`), and the member-side Reviews tab explicitly relies on being able to read back a private review it just submitted (the header literally reads "N Reviews **Visible to You**", meaning public + the caller's own regardless of visibility). But both RLS policies compared the raw, non-lowercased `auth.jwt() ->> 'email'` against the already-lowercased `email` column - `auth.jwt() ->> 'email' = email` and `... OR auth.jwt() ->> 'email' = email` respectively. Every other identity check in this codebase (job_board, community_events, one_on_one_logs, event_rsvps, and every table built after this one) consistently does `lower(auth.jwt() ->> 'email') = ...` for exactly this reason - this file (`005_reviews.sql`) is literally the earliest-numbered migration in the project, predating that convention. If a real Google account's JWT `email` claim ever comes back with any uppercase character (not something this app can control or guarantee against), every review submission from that account would be silently rejected by RLS on INSERT, and if one somehow got through, that member would never see their own private review in their own "Reviews Visible to You" list - it would just be invisible, indistinguishable from having failed to save.
- **Status**: Fixed - both policies now use `lower(auth.jwt() ->> 'email') = email`, matching the rest of the codebase's established convention exactly. Applied live via `npx supabase db query --linked`; verified via `pg_get_expr` on `pg_policy` that both live policy definitions now lowercase. No JS changed - `npm run build && npm run lint` still at baseline (37 problems, 36 errors, 1 warning).

Everything else read in this pass held up: Job Board's "no moderation, member listings go live immediately" design (documented and consistent - `addJobListing()`/RLS INSERT policy self-attributes `created_by` server-side, matching the reviews/events/cert-calendar pattern); `deleteJobListing()`'s admin-only RLS gate (widened to `community_manager` too in `067_permission_scopes.sql`'s PART 4, already verified in the earlier permission-scope pass); the admin Job Board tab's optimistic-delete-with-rollback-on-failure (a harmless cosmetic quirk: on a failed delete, the listing reappears at the end of the list rather than its original position - not worth a fix). One cross-reference worth noting for whoever eventually claims the **Push notification edge functions** checklist item: `push-new-job` (meant to notify members of a new Job Board listing) is never actually invoked anywhere in `src/` - dead code, consistent with the broader unfinished/unconfigured state of the push-notification feature already flagged in this session's own chat history (missing secrets, missing `google_oauth_tokens`/`push_tokens` tables).

**Next up**: Still unclaimed - **Community Content**, **Team & Roles**, **Dashboard (member home tab)**, **Members directory**, **1-on-1 Meetings**, **Resources**, **Breakdowns**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (push notification edge functions - note the dead `push-new-job` call site above, Gemma AI edge functions, cron jobs, referral program).

---

### 2026-09-12 (sixth pass, same day)

Claimed: **Community Content** (admin: broadcasts, wins, suggested content, Weekly Breakdowns management) + **Breakdowns** (member: archive list, markdown rendering, unsubscribe) - a natural pair since Weekly Breakdowns management is the admin half of the member Breakdowns tab. Read `068_weekly_breakdowns.sql`, `044_community_content.sql`, `045_suggested_content.sql`, the relevant slice of `067_permission_scopes.sql`, `src/lib/breakdownsData.js`, `src/lib/communityContentData.js`, `src/lib/renderMarkdown.js`, and both tabs in `AdminDashboard.jsx`/`MemberPortal.jsx`. Both checklist boxes checked off.

#### [Community Content] An Approved or already-Sent weekly breakdown had no server-side protection from being edited or deleted
- **Severity**: Medium-High
- **Where**: `supabase/068_weekly_breakdowns.sql` - `"staff manage breakdowns"` RLS policy (pre-fix)
- **What's wrong**: `updateBreakdown()`/`deleteBreakdown()` (`breakdownsData.js`) are documented "edit a Draft's plan fields" / "only makes sense for a Draft," and the admin UI only ever renders Edit/Delete for a Draft row (`AdminDashboard.jsx`, `{b.status === 'Draft' && (...)}`). But the RLS policy backing both has no status condition at all - the same "client-side gate, no server-side mirror" shape as the Projects-completion-bypass bug fixed earlier in this project's QA history. A Sent edition is what members already read on the Breakdowns tab and received by email; nothing stopped a direct write from silently rewriting or deleting it after the fact. No live symptom yet (0 Approved/Sent rows exist today - the feature is new), but the first real Friday send would have been exposed to this immediately.
- **Status**: Fixed (commit `4d1d688`) - a `BEFORE UPDATE OR DELETE` trigger now blocks any content-column change or a delete once `status <> 'Draft'`; the three status-transition RPCs (approve/unapprove/mark-sent) are unaffected since they only ever touch `status`/`approved_*`/`sent_at`/`recipient_count`/`broadcast_id`. Verified live with a disposable test row: approved it (status-only change, allowed), confirmed a content edit and a delete were both rejected with a clear error, confirmed pulling it back to Draft restored both, then cleaned up.

#### [Community Content] Suggested Content never got the same Community Manager widening as its two admin-page siblings
- **Severity**: Medium
- **Where**: `supabase/045_suggested_content.sql` - `"admins manage suggested content"` (pre-fix)
- **What's wrong**: This table's own header comment describes it as "the same admin-authored/member-read-only shape as community_broadcasts/community_wins" - both already widened to `community_manager` in `067_permission_scopes.sql`. This one never was. All three are managed from the exact same admin page (`AdminDashboard.jsx`, case `'community-content'`, whose own intro text explicitly promises all three are "editable here") - a community_manager could already use the Community Broadcast and Recent Wins forms on that page, but would hit a silent RLS-denied error submitting the Suggested Content form sitting directly below them on the same screen. Same finding-shape as the Quiz Duels widening gap fixed earlier this same day.
- **Status**: Fixed (commit `3dcc50d`) - widened in `067_permission_scopes.sql` (not `045` directly - `is_community_manager()` doesn't exist yet at `045`'s point in migration order, same constraint `019_events.sql` already documents for `approve_community_event()`), matching the exact convention already used for every other post-hoc widening in that file. Applied live via `npx supabase db query --linked`; verified via `pg_get_expr` on `pg_policy` that the live policy now includes `is_community_manager(auth.uid())`.

Everything else read in this pass held up: `renderMarkdown()`'s DOMPurify sanitization on `body_md` before `dangerouslySetInnerHTML` (a compromised CM/admin account still can't inject a script, by design); the member Breakdowns tab's archive-list/detail-pane split and its "no breakdowns yet" empty state; `unsubscribe_from_breakdown_emails()`'s anonymous cold-click shape (no in-app resubscribe/status toggle exists anywhere for this, but that's consistent with its two siblings - `roadmap_reminder_opted_out`, `linkedin_reminder_opted_out` - neither of which has in-app UI either, so this reads as a deliberate, established pattern across all three email types rather than a one-off gap); `get_breakdown_recipients()`/`get_breakdown_facilitator_emails()`'s admin-or-service-role gating.

**Next up**: Still unclaimed - **Team & Roles**, **Dashboard (member home tab)**, **Members directory**, **1-on-1 Meetings**, **Resources**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (push notification edge functions - note the dead `push-new-job` call site from the previous pass, Gemma AI edge functions, cron jobs, referral program - note also `weekly-breakdown-email`/`breakdown-nudge`/`breakdown-unsubscribe` edge functions themselves were not read this pass, only the DB/RLS/UI they sit on top of).

---

### 2026-09-12 (seventh pass, same day)

Claimed a wider sweep of smaller, mostly-independent areas in one go given the volume still unclaimed: **Dashboard** (member home tab), **Onboarding sequence + Getting Started hard gate**, **Offboarding sequence**, **Passkey sign-in + Security panel**, **Members directory**, **Referral program**. Read `032_login_streak.sql`, `064_recommended_rooms.sql` (cross-checked against its `067` widening), `006_onboarding.sql`, `008_offboarding.sql`, `010_member_directory.sql`, `039_referrals.sql`, `src/lib/passkeyData.js`, `src/components/SecurityPanel.jsx`, `src/components/SurveyBanner.jsx`, `src/components/OffboardingSequence.jsx`, `src/lib/offboardingData.js`, `src/lib/referralsData.js`, and the corresponding tabs/render sections in `App.jsx`/`MemberPortal.jsx`. All six checklist boxes checked off - no bugs found in any of them.

No findings this pass - everything read held up under a skeptical pass:
- **Dashboard**: `nextCommunityEvent` correctly filters to Approved + upcoming-only (matches the Events tab's own already-fixed pattern); `recommended_rooms` RLS *is* correctly widened to `community_manager` in `067` (only the original `064` file looked admin-only in isolation); `SurveyBanner` is a clean, self-contained, time-boxed component.
- **Onboarding**: the 3-day-grace hard gate's backfill logic (giving already-onboarded members a fresh grace period on rollout, rather than instantly hard-locking everyone past 3 days old) is careful and correct; `mark_my_onboarding_step_complete()` pins `step_key` to a fixed CHECK-constrained vocabulary and `completed_at` to the server clock, never trusting either from the client. Noted, not a bug: the hard gate's tab restriction (`MemberPortal.jsx`, `['dashboard','meetings','members']`) is enforced client-side only (a `useEffect` snap-back), but every other tab it would reveal exposes nothing more sensitive than what those three allowed tabs already show, so this is a soft UX nudge, not a security boundary, consistent with how this codebase treats every other purely-cosmetic tab gate.
- **Offboarding**: `submit_exit_feedback()` is tightly scoped (own row, only out of `'Leaving'`, idempotent), and `handleExitDone` (`App.jsx`) immediately signs the member out after finalizing to `'Left'`, so there's no stale-state window where a "just left" member could still see the portal.
- **Passkey/Security**: this app is a thin wrapper around Supabase Auth's own built-in WebAuthn API (`supabase.auth.signInWithPasskey/.registerPasskey/.passkey.*`) with no custom table or RLS of its own to audit; a passkey always resolves to the same `auth.users` row as the member's existing Google identity (registration requires an already-confirmed session), so every downstream check (`is_member_allowed`, role, RLS) is correctly provider-agnostic with nothing passkey-specific to bypass.
- **Members directory**: `010_member_directory.sql` is already the most heavily-hardened file in the schema by its own history (multiple past security-fix rounds folded back in) - `get_member_directory()`'s column whitelist correctly excludes `age`/`gender` (deliberately peer-invisible, readable only via the separate `get_my_age_and_gender()` own-row getter) and every other genuinely sensitive column (`money_owed`, `phone`, offboarding fields); `update_my_directory_profile()` is UPDATE-only (can't self-provision a new row) and scoped to exactly the same public-facing column set.
- **Referral program**: self-service INSERT/SELECT correctly scoped to the caller's own `referrer_email`; `REFERRAL_REWARD_AMOUNT` is purely a display constant (column header, "Refer a Friend" badge) with no automated expense-recording tied to a `'Reward Paid'` status change - a manual bookkeeping step, which reads as the intended design rather than a gap.

**Next up**: Still unclaimed - **Matchmaker**, **1-on-1 Meetings**, **Resources**, **Insights**, **Team & Roles**, and the whole **Cross-cutting/backend** section (email edge functions still-unread list: `breakdown-nudge`, `overdue-1on1-digest`, `linkedin-post-reminder-email`, `roadmap-reminder-email`, `push-1on1-reminder`, `weekly-breakdown-email`; push notification edge functions - note the dead `push-new-job` call site; Gemma AI edge functions; cron jobs).

---

### 2026-09-12 (eighth pass, same day)

Claimed the remaining member/admin-facing areas in one sweep: **Matchmaker**, **1-on-1 Meetings**, **Resources**, **Insights**, **Team & Roles**. Read `030_matchmaker.sql` (cross-checked its Community Manager widening in `067`), `src/lib/matchmakerData.js`, `supabase/functions/gemma-review/index.ts` + `gemma-interview-prep`'s auth/rate-limit shape, the 1-on-1 Meetings tab (external mentor-calendar links, correctly using hardcoded non-DB URLs so no `isSafeUrl()` gap), `026_resources.sql`, the full Insights tab's tenure/cert/meeting-cadence math in `AdminDashboard.jsx`, and `067_permission_scopes.sql`'s `get_my_role()`/`set_member_role()`/`list_team_members()` plus the Team & Roles admin tab. All five checklist boxes checked off.

#### [Team & Roles] set_member_role() could demote the last remaining admin, with no recovery path
- **Severity**: Medium-High
- **Where**: `supabase/067_permission_scopes.sql` - `set_member_role()` (pre-fix)
- **What's wrong**: The Team & Roles "Assign Role" form (`AdminDashboard.jsx`) is a free-text email field with a role dropdown, submitted straight to `set_member_role()` - unlike the roster list just below it, whose own "Revoke" button already protects against this exact mistake by hiding itself for any row with `role = 'admin'`. A mistyped or pasted email in that free-text form (most plausibly the founder's own, out of habit) with a lesser role selected would silently demote it, with no confirmation dialog and, until now, no server-side guard either. Confirmed live: exactly one `admin` account exists today - if it were ever demoted this way, nobody would be left who could call `set_member_role()` (founder-only) to undo it, short of a direct database fix.
- **Status**: Fixed (commit `e457aa0`) - the function now blocks only the specific case that actually strands the system: demoting the current last remaining admin (`SELECT count(*) ... WHERE role = 'admin'`, checked only when the target's current role is `'admin'` and the new role isn't). Demoting an admin while at least one other admin still exists is unaffected - this is a floor, not a lock on ever changing an admin's role. Applied live; verified via `pg_get_functiondef` that the live definition matches (a full auth-context E2E test wasn't attempted - would require impersonating the founder's real JWT, which this pass didn't do - but the guard logic itself is a simple, directly-inspectable count check).

Everything else read in this pass held up: **Matchmaker**'s group-sizing algorithm (`mod(rn-1, ceil(total/4.0))`) was hand-checked against N=2 through 13 and always produces valid 2-4-sized groups, never a 4+4+1-style leftover; `run_matchmaker_round()`/`matchmaker_optins`/`matchmaker_groups`/`matchmaker_group_ratings` are all correctly widened to `community_manager` in `067` (only the original `030` file looks admin-only in isolation - matches the same false-alarm shape as `recommended_rooms` from the previous pass, so worth remembering this pattern before flagging a table as "never widened" without checking `067` too); `matchmaker_group_ratings` has zero direct member table grants by design (RPC-only, same as `portal_events`), so anonymity is server-enforced, not just UI-hidden. **1-on-1 Meetings**: mentor booking links are hardcoded module constants, not DB-sourced, so the missing `isSafeUrl()` wrap there is correctly not a gap. **Gemma AI edge functions** (partial - `gemma-review`/`gemma-interview-prep` only, `gemma-chat` not read this pass): both share the same hardening shape - real JWT auth check, `is_member_allowed()` check via service-role client, a weekly usage cap, an input-length cap, and a service-role-only insert with no client-facing write policy. **Insights**: the tenure/cert/meeting-cadence calculations correctly benefited from this session's earlier two-pass `memberRosterMap` fix (a refund can no longer corrupt `lastPaymentDate`, which several Insights stats read from) with no further changes needed here.

**Next up**: Only the **Cross-cutting/backend** section remains unclaimed in full: **Email edge functions** (`breakdown-nudge`, `overdue-1on1-digest`, `linkedin-post-reminder-email`, `roadmap-reminder-email`, `push-1on1-reminder`, `weekly-breakdown-email` - none read yet), **Push notification edge functions** (note the dead `push-new-job` call site found two passes ago), **Gemma AI edge functions** (`gemma-chat` specifically, still unread), and **Cron jobs** (schedule correctness, `CRON_SECRET` checks, idempotency if a job fires twice). This is the last unchecked section on the whole coverage checklist.

---

### 2026-09-12 (ninth pass, same day - closes out the coverage checklist)

Claimed the entire remaining **Cross-cutting/backend** section: **Email edge functions**, **Push notification edge functions**, **Gemma AI edge functions**, **Cron jobs**. Read every remaining unread edge function (`weekly-breakdown-email`, `breakdown-nudge`'s auth shape, `gemma-chat`'s auth/rate-limit shape - `push-1on1-reminder`/`push-streak-alert`/`overdue-1on1-digest`/`sync-last-1on1-dates` were already read in earlier sessions/passes), cross-checked every edge function's live `verify_jwt` deployment flag against its own header comment via `npx supabase functions list`, and inspected every live `cron.job` row's actual `command` column (not just the migration files that originally scheduled them) for a mismatch between what's committed and what's actually running. This last check is what caught both findings below - neither was visible from reading the SQL files alone. All four checklist boxes checked off; this closes out the entire coverage checklist.

#### [Push notification edge functions] push-1on1-reminder and push-streak-alert were deployed without --no-verify-jwt, so their crons had been failing every single run
- **Severity**: High
- **Where**: Live Supabase Edge Function deployment config (not a file in this repo - the mismatch is between `supabase functions deploy <fn> --no-verify-jwt`, which each function's own header comment explicitly documents as the required deploy command, and however they were actually deployed)
- **What's wrong**: Both functions are triggered exclusively by pg_cron (`push-1on1-reminder-15min` every 15 minutes, `push-streak-alert-daily` nightly at 18:00 UTC), which authenticates purely via an `x-cron-secret` header the function checks itself - there is no Supabase-issued JWT on a cron-originated request at all. Both functions were live with `verify_jwt: true`, meaning Supabase's own gateway rejected every single invocation with a 401 ("Missing authorization header") before the function's own code - including its `x-cron-secret` check - ever ran. Confirmed live via `net._http_response`: `push-1on1-reminder-15min` had been failing this way every 15 minutes with no gap, all day, every day since it was first deployed.
- **Status**: Fixed - redeployed both via `npx supabase functions deploy push-1on1-reminder --no-verify-jwt` and `... push-streak-alert --no-verify-jwt`; confirmed live via `npx supabase functions list` that `verify_jwt` is now `false` for both. Note: fixing this exposed the *next* problem for both (see the finding immediately below) - and even with both problems now fixed, this specific pair still won't do anything useful yet, since the broader push-notification feature (`google_oauth_tokens`/`push_tokens` tables, `FCM_SERVICE_ACCOUNT`/`GOOGLE_TOKEN_ENCRYPTION_KEY`/`GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET` secrets) is still genuinely unfinished/unconfigured, already known from earlier this session. Confirmed live post-fix: `push-streak-alert` now correctly reaches its own code and fails with a clean, informative `"Not configured"` (missing `FCM_SERVICE_ACCOUNT`) instead of a silent gateway rejection - a real improvement (the failure is now visible and diagnosable in the function's own logs) even though the feature itself needs real Firebase credentials before it can actually send anything, which is not something to fabricate or configure unilaterally.

#### [Cron jobs] Three live cron jobs were sending the literal, never-replaced placeholder string "<YOUR_CRON_SECRET>" instead of the real secret - they have never worked, ever
- **Severity**: Critical
- **Where**: Live `cron.job` rows for `push-1on1-reminder-15min`, `push-streak-alert-daily`, and `linkedin-post-reminder-weekly` (each function's own migration file - `049_push_notification_crons.sql` for the first two - correctly instructs "replace `<YOUR_CRON_SECRET>` below with the exact value already set" before running; whoever scheduled these three specific crons against the live database never did)
- **What's wrong**: Read directly off the live `command` column (not visible from the migration files, which are correct - the placeholder text is the *intended* unfilled template, exactly like the still-unfilled `<YOUR_CRON_SECRET>` sitting in this repo's own `072_overdue_1on1_digest_cron.sql`/`074_calendar_sync_cron.sql` today, both of which *were* correctly filled in with the real secret when actually run against the live database earlier this session). These three, though, were run against the live database with the placeholder text never swapped out - meaning every scheduled firing of all three sent the literal string `"<YOUR_CRON_SECRET>"` as the `x-cron-secret` header, which obviously never matches the real `CRON_SECRET`. Confirmed live: manually triggering `push-1on1-reminder` with the real secret got as far as the function's own code (a real `"Unauthorized"` from its own `x-cron-secret` check, not the gateway's `"Missing authorization header"`) only *after* the `verify_jwt` fix above - before that fix, the gateway-level 401 was masking this second, independent bug entirely. `linkedin-post-reminder-weekly` had `verify_jwt: false` (correctly deployed) but was *still* completely dead the whole time, purely from this placeholder-secret bug alone - a real, silently-broken weekly feature with no relationship to the push-notification infrastructure gap at all.
- **Status**: Fixed - rescheduled all three via `cron.unschedule()` + `cron.schedule()` with the real secret safely extracted from an already-correct working cron job's own `command` column (the same safe pattern used earlier this session for `072`/`074`, never typed by hand), preserving each job's original schedule exactly. Verified live: all three `command` columns no longer contain the placeholder. **Directly confirmed the fix actually restored real functionality**: manually triggering the now-fixed `linkedin-post-reminder-email` returned `{"candidates":8,"sent":8,"failed":[]}` - a genuine send to 8 real members, not a dry run. This means the fix caused 8 real LinkedIn-posting-reminder emails to go out today, outside the normal Tuesday-morning cadence, as a direct (and unavoidable, given the only way to verify a real Resend send succeeded is to actually let it send) side effect of confirming this fix works - worth knowing about, though low-stakes: a routine, expected reminder email these members already receive weekly, just one occurrence slightly off-schedule.

Everything else read in this pass held up: `weekly-breakdown-email`'s idempotency reasoning (a stuck-`'Approved'`-if-`mark_weekly_breakdown_sent`-fails edition can't actually double-send on a future Friday, since `send_date` is `UNIQUE` and a future Friday's date will never match a stuck older edition's date - the function's own comment slightly overstates this risk, but it's a harmless inaccuracy in a log line, not a real bug); `breakdown-nudge`'s auth/secret shape (its live cron already had the real secret, unaffected by either bug above); `gemma-chat`'s auth check + daily message cap, matching the same hardening shape already verified for `gemma-review`/`gemma-interview-prep`; every other live cron job's `command` column was individually checked and found correct (`roadmap-reminder-email-daily`, `weekly-breakdown-friday`, `breakdown-nudge-wednesday`, `overdue-1on1-digest-daily`, `calendar-sync-last-1on1-daily` all have the real secret; `resolve-expired-duels-hourly`/`assign-room-race-rooms-daily` call plain SQL functions directly with no HTTP/secret involved at all, so neither bug class can apply to them).

**This closes out every item on the coverage checklist.** Every member-facing and admin-facing area, plus the full cross-cutting/backend section, has now been through at least one QA pass. Genuinely still-open items across the whole log, for whoever picks this project back up: the Cert Calendar display-name spoofing question (needs a founder product decision), the mentor-UI gap (a real feature build, not a bug), the quarterly-competition-reset mechanism (built, deliberately not yet fired), the "Tegranyota2004" orphan-refund data anomaly (needs a manual look, not a code fix), and the broader push-notification feature's genuine incompleteness (missing tables/Firebase secrets - the two bugs fixed this pass make its failures correctly *visible* again, not functional).

---

### 2026-09-12

Claimed: **RLS/permission-scope audit across `067_permission_scopes.sql`** - the last major cross-cutting area every prior pass flagged as a top pick and none had claimed yet. Read the whole file (all 4 parts: role infrastructure, Community Manager widening, Mentor widening, the later Job Board/Events follow-up) plus the original/base migration each widened table or RPC came from (`002_member_persistence.sql` for `is_admin()`, `019_events.sql` for `approve_community_event()` and the event-images bucket, `015_competition_standings.sql`/`016_days_logged.sql`/`053_competition_opt_out.sql` for the Competitions tables, `062_quiz_duels.sql`/`063_room_races.sql`/`066_live_trivia.sql`, `028_roadmap.sql`, `055_cv_reviews.sql`/`056_interview_prep.sql`/`058_member_interviews.sql`, `025_job_board.sql`, `024_cert_calendar.sql`), then cross-checked every widened policy/RPC against `src/components/Sidebar.jsx`'s per-role menu (`menuItems` by `viewRole`, ~line 404-452) and `src/views/Admin/AdminDashboard.jsx`'s tab content to see whether each grant is actually reachable, over-reachable, or missing.

#### [Permission scopes] `approve_community_event()` let every admin approve events, not just siya or a Community Manager - contradicting its own design intent, documented three separate times
- **Severity**: High
- **Where**: `supabase/067_permission_scopes.sql` - `approve_community_event()` (~line 618, pre-fix)
- **What's wrong**: `is_community_manager(uid)` (this file's own PART 1, ~line 65) deliberately resolves `TRUE` for `role = 'admin'` too, not just `'community_manager'` - a convenience so the `is_admin() OR is_community_manager()` idiom used everywhere else in this file reads cleanly (every other call site explicitly ORs in `is_admin()` anyway, so that inclusiveness is harmless there). `approve_community_event()` was the one place that used `is_community_manager()` alone, as the entire second half of an exclusivity check: `IF email != siya AND NOT is_community_manager() THEN RAISE EXCEPTION`. Because `is_community_manager()` already says "yes" for any admin, this let every admin account approve a member-submitted event - exactly the access this RPC exists to prevent. Three separate comments all documented the intended behavior and were all silently wrong: `019_events.sql`'s original header ("only siya@hackinghub.co.za can approve... deliberately narrower than the general `is_admin()` check"), this file's own PART 4 header ("widened... to also accept a community_manager, alongside siya specifically, not instead of her"), and the paragraph directly above the function ("now exactly siya@hackinghub.co.za OR any community_manager, not every admin"). `AdminDashboard.jsx`'s `canApproveEvents` (~line 1892) already implements the *correct*, narrower check client-side and its own comment says it's "mirrored here so the button/explanatory text match what the RPC will actually allow" - so the UI never exposed the button to a non-siya admin, but anyone with a Google account promoted to plain `admin` could still call the RPC directly (browser console) and approve any event. Checked live role counts: today there's exactly one `admin` account (siya herself) and 4 `community_manager`s, so this had zero real-world exploitation surface *today* - but it would have mattered the instant a second admin account was created, silently reopening a door the original design went out of its way to keep shut.
- **Status**: Fixed (commit `9cdcadc`) - the function now checks `profiles.role = 'community_manager'` directly via an inline `EXISTS`, matching the exact idiom `is_admin()`/`is_community_manager()` themselves use, instead of going through the inclusive helper. Applied live via `npm run db:apply`; verified via `pg_get_functiondef` that the live definition matches. `npm run build && npm run lint` unchanged from baseline (36 errors, 1 warning - pure SQL change, no JS touched).

#### [Permission scopes] File's own top-of-file header contradicted its own PART 4 about Job Board
- **Severity**: Cosmetic
- **Where**: `supabase/067_permission_scopes.sql` - header comment (~line 29-34, pre-fix) vs. PART 4 (~line 576+)
- **What's wrong**: The header (written when the file was first created) listed Job Board as "deliberately NOT widened... stays founder-only." PART 4, added later in the same file as a follow-up, does widen `job_board`'s RLS policy to `community_manager`, with its own comment confirming that was a deliberate, founder-approved addition - the header paragraph was just never updated when that landed, leaving two flatly contradicting claims about the same table in one file. Exactly the doc-vs-code mismatch this project's QA process exists to catch, just found in SQL comments instead of `CHANGELOG.md`/`releaseNotes.js` this time.
- **Status**: Fixed (commit `470a705`) - header now correctly excludes Job Board from the founder-only list and notes it was widened in PART 4 instead. Also noted in passing that `reviews` isn't mentioned in that list at all (it stays founder-only by omission, not by an explicit call-out) - left as-is, since `reviews` genuinely shouldn't be widened (see finding below) and the omission doesn't cause any wrong behavior, just an incomplete paragraph.

#### [Permission scopes] Quiz Duels never got the same Community Manager widening its sibling competition systems (Room Races, Live Buzzer Trivia) did
- **Severity**: Low
- **Where**: `supabase/062_quiz_duels.sql` - `"admins manage duel questions"` (`duel_questions`), `"admins manage duels"` (`quiz_duels`), `"admins manage duel answers"` (`quiz_duel_answers`) - all still `USING (public.is_admin(auth.uid()))` only, never touched by `067_permission_scopes.sql`
- **What's wrong**: `062_quiz_duels.sql`, `063_room_races.sql`, and `066_live_trivia.sql` are three peer "head-to-head competition" systems living under the same Competitions tab that Community Manager's stated purpose ("runs competitions") clearly covers. `067` widened Room Races (`room_races`, `approve_room_race_submission()`) and Live Buzzer Trivia (`trivia_sessions`/`trivia_participants`/`trivia_buzzes` plus all four hosting RPCs) to `community_manager`, but Quiz Duels' three tables were left admin-only with no explanation anywhere in `067` for the asymmetry. In practice this is currently inert either way - there is no admin-facing UI at all for any role (checked `AdminDashboard.jsx`: no import of `quizDuelData`, no `quiz_duels`/`duel_questions` reference anywhere) to manage duels or seed `duel_questions` directly; duels are fully peer-resolved (`challenge_to_duel()`, `submit_duel_answer()`, `resolve_expired_duels()`) with no admin intervention point exposed. So nobody - CM or admin - is blocked from anything they currently use. But the inconsistency means the moment anyone builds a duel-questions admin UI (someone has to seed `duel_questions` some way today - unclear how, possibly a direct SQL insert done by hand), it will silently default to admin-only unless whoever builds it remembers this gap.
- **Is this intentional?** Unclear - could be a deliberate "Duels specifically need no human moderation, unlike Races (room assignment) and Trivia (live hosting)" stance, since Duels genuinely has no manual step Room Races/Trivia have. Or could just be an oversight (three sibling systems, two got the follow-up, one didn't). Founder asked to fix it (2026-09-12) rather than leave it open.
- **Status**: Fixed (commit `2e14ad8`) - all three tables widened to `is_admin() OR is_community_manager()`, matching Room Races/Trivia exactly. Zero live behavior change today (still no admin UI touches these tables), but the role matrix is now consistent for whenever one is built.

#### [Permission scopes] Mentor's stated "CV review, interview prep" purpose has no UI mentors can actually reach
- **Severity**: Medium
- **Where**: `supabase/067_permission_scopes.sql` PART 3 widens `cv_reviews` (`055_cv_reviews.sql`), `interview_prep_sessions` (`056_interview_prep.sql`), and `member_interviews` (`058_member_interviews.sql`) to `is_mentor()`; the only UI surface that reads any of this (a read-only real-interviews list) lives inside `src/components/MemberProfileModal.jsx`, opened from the Members tab in `AdminDashboard.jsx`
- **What's wrong**: The file's own header describes `mentor` as existing to "review roadmap/cert/CV/interview progress," and dutifully widens the three tables that back CV Review, Interview Prep, and real-interview tracking. But `src/components/Sidebar.jsx`'s per-role menu (~line 397-452) gives `mentor` only `dashboard`, `roadmaps`, and `certifications` - no `members` tab at all, same as `community_manager`, and for the same documented reason (`member_profiles` bundles `money_owed`/`phone`/`age` with no column-level RLS, so there's no safe way yet to expose the roster without also exposing the money - this exact gap is already called out in `067`'s own header as a deliberate, known limitation). The practical result: even though the RLS layer genuinely grants a mentor read/write access to `cv_reviews`/`interview_prep_sessions`/`member_interviews`, there is currently no button, tab, or screen anywhere in the shipped app - reachable by a mentor - that lets them look at a member's CV review score, generated interview-prep questions, or logged real-interview history. The one UI that shows any of this (`MemberProfileModal`'s read-only real-interviews list) is bolted onto the admin-only Members tab a mentor can't open. So a full third of what this role was created to do ("CV review, interview prep") is, today, backend-only - a mentor account can query it via a direct Supabase client call, but never through the app itself.
- **Is this intentional?** Almost certainly an acknowledged, deliberate gap rather than a fresh oversight - `067`'s own header already flags the underlying blocker (no column-level RLS / whitelist function for the roster) as a known limitation, and this finding is really just that same gap's concrete consequence for the mentor role specifically, not a new discovery. Worth surfacing anyway since "the RLS is technically granted" and "a mentor can actually do their job in the app" are two different claims, and only the first one is currently true.
- **Status**: Logged, needs a product/engineering decision - not fixed (building a narrow, non-financial "member growth" read view scoped to `cv_reviews`/`interview_prep_sessions`/`member_interviews`/`roadmap_items` that a mentor could actually reach is a real feature build, not a bug fix, and out of scope for a QA pass to decide unilaterally).

#### [Permission scopes] `competition_standings`'s own direct-table admin policy was never widened to Community Manager
- **Severity**: Cosmetic
- **Where**: `supabase/015_competition_standings.sql` - `"admins manage competition standings"` (~line 39), still `USING (public.is_admin(auth.uid()))` only
- **What's wrong**: Noted for completeness since Community Manager's whole purpose is "runs competitions." In practice this is harmless - there is no UI (admin or CM) that writes to `competition_standings` directly; every real write path (`review_daily_room_log()`, `correct_room_log_review()`, `rsvp_for_competition()`, `start_new_competition()`) is a `SECURITY DEFINER` RPC that's already correctly widened to CM independently of this table-level policy. This would only matter if a future "manually edit a member's standings row" admin feature got built without also remembering to widen this specific policy.
- **Status**: Fixed (commit `2e14ad8`) - widened to `is_admin() OR is_community_manager()`, matching every sibling policy in this same section. Founder asked to close it out alongside the Quiz Duels gap above rather than leave both open.

Everything else read in this pass matched the intended role matrix cleanly: `daily_room_logs`/`review_daily_room_log()`/`correct_room_log_review()`, `room_races`/`approve_room_race_submission()`, `matchmaker_optins`/`matchmaker_groups`/`matchmaker_group_ratings`/`run_matchmaker_round()`, `community_broadcasts`/`community_wins`/`recommended_rooms`, `trivia_sessions`/`trivia_participants`/`trivia_buzzes` + all four hosting RPCs, `roadmap_items`/`review_project_submission()`, `cert_calendar` (correctly dual-widened to both mentor and CM, founder-confirmed per its own comment), `cv_reviews`/`interview_prep_sessions`/`member_interviews` (RLS-correct even though currently UI-unreachable, see above), `job_board`, `community_events`/`event_rsvps`/the event-images storage bucket policy - each is reachable exactly by the role(s) `Sidebar.jsx`'s menu and `AdminDashboard.jsx`'s tab content say it should be, no more and no less (aside from the two bugs above). The deliberately-NOT-widened list (member financial fields, Finances, Payments & Subs, Merch Orders, Insights, role assignment) all correctly stay founder-only client- and server-side. `reviews` (member feedback/criticism table, `005_reviews.sql`) also correctly stays founder-only even though `067` never explicitly says so - it can contain private criticism of specific staff members, so keeping it out of CM/mentor hands (who could themselves be the subject of a review) is the right call, just undocumented.

**Next up**: This closes out the last major cross-cutting area every prior pass flagged. Remaining unclaimed checklist items, roughly by size: **Dashboard** (member home tab - untouched so far), **Members directory**, **1-on-1 Meetings**, **Job Board (member view)**, **Resources**, **Breakdowns**, **Cert Calendar (member + admin result-marking/email trigger)**, **Reviews (member view)**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, and the whole **Cross-cutting/backend** section (email edge functions, push notification edge functions, Gemma AI edge functions, cron jobs, Referral program) - none of these have been claimed by any pass yet. Correction to this pass's own earlier chat summary, which incorrectly called the PayFast-checkout amount-tampering bug still-open: it was already fixed on 2026-09-11 (commit `651eb5d`) and is correctly logged as Fixed further up this file - that finding's own entry was never touched by this pass and was misquoted in this pass's closing recap, not in this document. Also still open from earlier passes: the quarterly-competition-reset mechanism (already built, `start_new_competition()` deliberately not yet fired - waiting on the founder's own timing), and the mentor-UI gap logged just above (a real feature build, not something to decide unilaterally).

---

Claimed (second pass, same day): **Cert Calendar** (member: countdown/booking, Exam Readiness tracker, practice quizzes) + **Cert Calendar admin** (result marking, cert-pass email trigger) - the largest unclaimed item left, and one that pairs a member view with its admin counterpart the same way earlier passes did for Roadmap and Competitions. Read `024_cert_calendar.sql`, `051_exam_readiness.sql`, `054_quiz_system.sql`, the Cert Calendar slice of `067_permission_scopes.sql`, `supabase/functions/cert-pass-email/index.ts`, `src/lib/certCalendarData.js`, `src/lib/examReadinessData.js`, `src/lib/quizData.js`, `src/components/CertDetailsModal.jsx`/`ExamReadinessModal.jsx`, and the Cert Calendar tabs in both `MemberPortal.jsx` and `AdminDashboard.jsx`. Both checklist boxes are checked off - fully covered.

#### [Cert Calendar admin] cert-pass-email rejected every real community_manager/mentor whose Google account isn't @hackinghub.co.za - a stale pre-067 permission check
- **Severity**: Medium-High
- **Where**: `supabase/functions/cert-pass-email/index.ts` (pre-fix) - `callerEmail.endsWith('@hackinghub.co.za')`; same bug, same shape, found in `supabase/functions/matchmaker-group-email/index.ts` too
- **What's wrong**: Both edge functions predate `067_permission_scopes.sql` and were never updated when it shipped. Each is triggered directly from a live admin/staff session right after a privileged action succeeds (cert-pass-email after `announceCertWin` marks a Cert Calendar entry Passed; matchmaker-group-email after `run_matchmaker_round()`), and each gates that trigger by checking the *caller's own email domain* rather than their actual role - a leftover from before 067 existed, when `@hackinghub.co.za` and "admin" were the same thing. They weren't updated when 067 decoupled the two: `067`'s own header says "every new sign-in, regardless of domain, starts as a plain member - the founder assigns community_manager/mentor/admin explicitly afterward via `set_member_role()`", and `App.jsx`'s own comment confirms it moved to `get_my_role()` for exactly this reason ("NOT guessed from the email domain any more"). `cert_calendar`'s RLS policy is correctly widened to `is_admin() OR is_mentor() OR is_community_manager()`, and `run_matchmaker_round()` is correctly widened to `is_admin() OR is_community_manager()` - both roles really can perform the underlying action through the app exactly as intended. But neither edge function was told about that widening, so a real community_manager/mentor doing exactly what their role is meant for got the underlying action to succeed (Recent Win posted / matchmaker groups formed) and then hit a silent "Admins only" 403 on the follow-up email, surfaced to them as `setCertsError`'s/matchmaker's "...but the email failed to send" toast every single time - not a one-off glitch, a guaranteed failure for that account, forever. Checked live: queried `profiles` (read-only `SELECT email, role`) and confirmed 3 of today's 4 `community_manager` accounts (`fancymogale4@gmail.com`, `lesedincwana@gmail.com`, `thakgalangmphaila@gmail.com`) use personal Gmail addresses, not `@hackinghub.co.za` - so this wasn't a hypothetical, it was already live and reachable by most of the real community_manager roster.
- **Status**: Fixed (commit `2aa313f`) - both functions now call `get_my_role()` (the same RPC `App.jsx` itself uses) via the caller's own authenticated client and check the actual role (`admin`/`community_manager`/`mentor` for cert-pass-email, matching `cert_calendar`'s RLS; `admin`/`community_manager` for matchmaker-group-email, matching `run_matchmaker_round()`'s guard) instead of the caller's email domain. Both redeployed live via `npx supabase functions deploy`. Zero behavior change for the founder's own `@hackinghub.co.za` admin account (still passes, `role='admin'` either way); the fix only changes the outcome for the community_manager/mentor accounts that were being wrongly rejected.

#### [Cert Calendar] Member-facing countdown badge read "Exam Passed" for a Failed exam, on a calendar the whole community sees
- **Severity**: Medium
- **Where**: `src/views/Member/MemberPortal.jsx` - the Community Operatives Exam Countdown card render (~line 5635, pre-fix)
- **What's wrong**: The countdown badge's text and color were driven purely by `daysLeft` (days until `c.date`), with no check of `c.result` at all - unlike the admin Cert Calendar tab, which already correctly branches on `c.result === 'Failed'` to show its own red "Failed" badge. Once a cert's target date is in the past, the member-side badge falls to its last case and reads "Exam Passed" regardless of whether the entry was actually marked Passed, Failed, or is still sitting Pending past its date - so a member (or admin/CM/mentor) who marked someone's exam **Failed** would see that member's card keep showing a green-worded "Exam Passed" label forever, immediately next to a separate "Certifications Earned" section that uses the exact same word "Passed" to mean the opposite thing. This is a shared, community-wide calendar (every member's entries are visible to everyone), so the mislabel wasn't a private confusion - it read, to the whole community, like a mismarked win.
- **Status**: Fixed (commit `2aa313f`) - the badge now checks `c.result === 'Failed'` first and shows a red "Failed" badge, matching the admin side's existing pattern exactly; a still-`Pending` entry whose date has elapsed now reads "Awaiting Result" instead of "Exam Passed", so the word "Passed" on this tab only ever appears when a result genuinely is Passed. Pure JSX/copy change, no schema or RLS involved. Also corrected three stale doc-comments in `src/lib/certCalendarData.js` that still said "Admin-only" for functions `067_permission_scopes.sql` had already widened to community_manager/mentor.

#### [Cert Calendar] Self-submitted entry's display name is free-text and entirely client-trusted - a member can attribute a cert-calendar entry to any name, including another real member's
- **Severity**: Medium
- **Where**: `supabase/024_cert_calendar.sql` - `"members add cert calendar entries"` INSERT policy; `src/views/Member/MemberPortal.jsx` - the "Your Name" field in the Add to Cert Calendar form (~line 5783), submitted via `handleAddCertEntry`/`addCertCalendarEntry()`
- **What's wrong**: The INSERT policy correctly locks `created_by` and `member_email` to `lower(auth.jwt() ->> 'email')` - a member can't fake *whose* submission it is for anything identity-sensitive - but the `member` column (the actual display name rendered on this shared, community-wide calendar) has no such constraint at all. The Add form's "Your Name" field starts empty and is sent to the database exactly as typed, with nothing tying it to the signed-in member's real profile name. Same shape as the RSVP/Duel/Room-Race opponent-name spoofing bugs found and fixed in the Competitions pass (`053_competition_opt_out.sql`, `062_quiz_duels.sql`, `063_room_races.sql`) - a member could type any string, including a real teammate's real name, and have it show up as if that person is targeting an exam date, visible to the whole community.
- **Is this intentional?** Genuinely unclear, and different in kind from the Competitions cases, which is why this one is logged rather than fixed outright: in every Competitions fix, the client already always sent the honest value automatically (the user never typed it themselves), so resolving it server-side changed zero legitimate behavior. Here, "Your Name" is a field the member fills in by hand on their own submission - locking it to `member_profiles.full_name` server-side (the obvious mirror-image fix) would silently overwrite anything a member deliberately typed differently (a preferred short name, a nickname), which is a real product/UX decision about whether this field is "your identity" (should be locked) or "how you want to appear" (should stay free but perhaps just gain a lighter guard, like rejecting a value that exactly matches a *different* real member's registered name). Not something to decide unilaterally.
- **Status**: Logged, needs a product decision - not fixed.

Everything else read in this pass held up cleanly: `exam_readiness`'s member-owned RLS and its two `update_my_exam_readiness_checklist()`/`log_my_practice_test_score()` RPCs (both correctly self-email-scoped, no client-trusted identity anywhere); `get_quiz_questions()`'s per-question shuffle math (`orig_idx`/`new_idx` remapping checked by hand against a sample shuffle - the remapped `correct_index` does track the shuffled choices correctly); `submit_quiz_attempt()` grading entirely server-side against `quiz_questions.correct_index` (a client-sent score would be ignored even if forged); the deliberate, documented choice to let members see `correct_index`/`explanation` up front in Exam Mode (practice content, not a proctored exam - the real gate is client-side UX, not RLS, and that's an accepted tradeoff, not a bug); the 50/50 readiness-percentage blend math; and the Insights "first cert by email, falling back to name" matching logic. `fetchAllExamReadiness()` (admin-only, powers Insights) correctly stays ungated to CM/mentor, consistent with Insights itself being admin-only in `Sidebar.jsx`.

**Next up**: Remaining unclaimed checklist items: **Dashboard** (member home tab), **Members directory**, **1-on-1 Meetings**, **Job Board (member view + admin)**, **Resources**, **Breakdowns**, **Reviews (member view + admin)**, **Onboarding/Offboarding sequences**, **Passkey sign-in + Security panel**, **Admin Overview**, **Members management**, **Finances**, **Community Content**, and the **Cross-cutting/backend** section (push notification edge functions, Gemma AI edge functions, cron jobs, Referral program - note the "Email edge functions" item can now be narrowed: `cert-pass-email` and `matchmaker-group-email` are covered by this pass, but `breakdown-nudge`, `overdue-1on1-digest`, `linkedin-post-reminder-email`, `roadmap-reminder-email`, `push-1on1-reminder`, `weekly-breakdown-email` are still unread). The Cert Calendar display-name spoofing finding just above is a good, small, self-contained pickup for whoever next has a founder answer on "should this be locked to the profile name."

### 2026-09-11

Claimed: **My Roadmap** (member) + **Roadmaps management** (admin) - a natural pair since they're two views onto the same `roadmap_items` data and unlock logic (`supabase/028_roadmap.sql`, `035_roadmap_foundations_approval.sql`, `src/lib/memberOptions.js`, `src/lib/roadmapData.js`, `src/views/Member/MemberPortal.jsx`, `src/views/Admin/AdminDashboard.jsx`).

#### [Roadmaps management] Admin checkbox let a Projects item be marked done without going through proof review
- **Severity**: Medium
- **Where**: `src/views/Admin/AdminDashboard.jsx` - `handleToggleRoadmapItemDone` (~line 468) and its checkbox button in the roadmap item list (~line 3557, pre-fix)
- **What's wrong**: The generic "mark done" checkbox rendered for every roadmap item regardless of phase, including Projects. Clicking it for a Projects item called `updateRoadmapItem()` directly, flipping `completed` but never touching `review_status`/`reviewed_at`/`review_note` - completely bypassing `review_project_submission()`. An admin could end up with `completed = true` while `review_status` stayed `'Not Submitted'` or `'Pending'` forever - the exact split state the Projects proof-review system (`028_roadmap.sql`) exists to prevent. `CHANGELOG.md`'s 2026.09.06 entry explicitly promises "a Projects-phase roadmap item no longer self-completes on click... only an approval ever flips the item to done" - this was a real regression against that documented guarantee, reachable from the admin side even though the member side (`handleToggleMyRoadmapItem` in `MemberPortal.jsx`) already had the equivalent guard.
- **Status**: Fixed (commit `b99dca3`) - the checkbox is now disabled for Projects-phase items (with a tooltip pointing at the Approve/Reject controls below), and `handleToggleRoadmapItemDone` bails out early on `phase === 'Projects'` as a defense-in-depth guard in case anything else calls it later.

#### [Roadmaps management] No admin path to approve a Project that was never submitted
- **Severity**: Low
- **Where**: `src/views/Admin/AdminDashboard.jsx` ~line 3570 - the Approve/Reject controls only render `{item.phase === 'Projects' && item.reviewStatus !== 'Not Submitted' && (...)}`
- **What's wrong**: `review_project_submission()` (the SQL function) has no restriction requiring `review_status` to already be `'Pending'` - an admin calling it directly against a `'Not Submitted'` item would work fine (sets `completed = true`, `review_status = 'Approved'`, `reviewed_at` recorded, `proof_url` stays NULL). But the UI never exposes Approve/Reject unless the member has first submitted a proof URL. So today, if an admin wants to manually credit a Project a member already did (e.g. before this feature existed, or proof was shared outside the app), there is no way to do it through the review flow at all - and after my fix above, the checkbox workaround is gone too, so there is now genuinely no UI path for this case.
- **Is this intentional?** Unclear - could be a deliberate "proof is mandatory, no exceptions" stance, or an oversight where the Approve/Reject buttons just weren't hooked up for the not-yet-submitted case. Founder asked to fix it (2026-09-11, later same day) rather than leave it open.
- **Status**: Fixed (commit `8ee3c19`) - a clearly-labeled "Approve Without Proof" control now renders for `'Not Submitted'` Projects items, visually distinct (secondary button, explicit wording) from the normal Approve/Reject pair so it still reads as the exception, not the default click.

Everything else read in this pass (unlock math for Specialization/Projects/Advanced, the `SPECIALIZATION_UNLOCK_MIN` vs `CORE_FOUNDATIONS_MIN_REQUIRED` two-tier gate, the foundations-approval RLS boundary on `member_profiles`, the auto-assign-Core-Foundations trigger on onboarding completion, the `enforce_linkedin_post_item_phase` trigger, the "gone quiet" reminder cadence math) matched its own documentation and held up under a skeptical read - no further bugs found there.

**Next up**: Nothing else claimed this pass. A good next pick from the checklist: **Competitions** (standings + tie-splitting logic, `031_daily_room_logs.sql`/`015_competition_standings.sql` are dense and haven't been read yet) or **My Subscription & Upgrades** (PayFast checkout money-handling, real financial impact if wrong) - both fit the "complex logic / money-handling" bar this project prioritizes. The RLS/permission-scope audit across `067_permission_scopes.sql` is also still fully unclaimed and cross-cutting.

---

Claimed (second pass, same day): **Competitions - daily room logs, standings + tie-splitting, and the rules guide** (member) + **Room Logs review** (admin), per the pick above. Read `015_competition_standings.sql`, `016_days_logged.sql`, `031_daily_room_logs.sql`, `053_competition_opt_out.sql`, the relevant slice of `067_permission_scopes.sql` (it turned out to own the *live* redefinition of `review_daily_room_log`, superseding 031's copy the same way 053 already superseded 015/016 for `rsvp_for_competition` - worth remembering for any future Competitions pass), `src/lib/competitionData.js`, `src/lib/roomLogData.js`, the Competitions tab + `computeCompetitionPrizes` in `MemberPortal.jsx`, `CompetitionRulesModal.jsx`, and the Room Logs admin tab in `AdminDashboard.jsx`. **Not covered**: Head-to-Head Duels (`062_quiz_duels.sql`), Room Races (`063_room_races.sql`), Live Buzzer Trivia (`066_live_trivia.sql`) - real, separate game systems, out of room for this pass. Leaving the **Competitions** checkbox unchecked for that reason; **Room Logs review** is checked off as fully covered.

#### [Competitions] Room log submitted before RSVPing silently never counted
- **Severity**: Medium
- **Where**: `supabase/031_daily_room_logs.sql` - `submit_daily_room_log()`
- **What's wrong**: The "Log Today's Rooms" form has no RSVP gate, but `review_daily_room_log()` credits `competition_standings` by `UPDATE ... WHERE email = v_member_email` - a silent no-op if that member never RSVP'd (no row to match). A member could submit, get an admin approval, see "Locked for the day", and never appear on the leaderboard, with zero error anywhere telling them why - a classic client-side-gate-missing-its-server-side-mirror bug, just inverted (no gate existed at all, anywhere).
- **Status**: Fixed (commit `c0c2436`) - `submit_daily_room_log()` now raises a clear exception pointing back at the RSVP button if the caller has no `competition_standings` row yet. Applied to the live DB via `npm run db:apply`.

#### [Competitions] Concurrent admin/Community Manager review could double-credit standings
- **Severity**: Medium-High
- **Where**: `supabase/067_permission_scopes.sql` - `review_daily_room_log()` (this file, not `031_daily_room_logs.sql`, owns the live definition - see claim note above)
- **What's wrong**: The function credited `competition_standings.rooms_completed`/`days_logged` on every `p_approved = true` call with no check that the log was still `Pending`. A single reviewer can't double-click into this (the admin UI removes Approve/Reject the moment a log leaves Pending), but two reviewers can - and since 067 widened room-log review to Community Managers alongside admins, two people having the same Pending log open at once and both clicking Approve is a realistic scenario, not a hypothetical. Each call would re-add `room_count` to the leaderboard, inflating a number that determines real prize-voucher money.
- **Status**: Fixed (commit `d95b233`) - the credit UPDATE is now scoped to `status = 'Pending'` with a `FOUND` check, which Postgres's row-level locking makes atomic: the second concurrent call sees the already-changed status, matches zero rows, and gets a clear "already reviewed" error instead of double-crediting. Applied to the live DB.

#### [Competitions] Tied leaderboard rows showed different ranks/medals despite splitting the same prize
- **Severity**: Low-Medium
- **Where**: `src/views/Member/MemberPortal.jsx` - the Current Standings table render (~line 6002-6070) and `computeCompetitionPrizes` (~line 2373)
- **What's wrong**: `computeCompetitionPrizes` correctly groups members with equal `rooms` into one tied group and splits their combined prize money evenly (and labels it "(tied)"). But the Rank column and medal color were driven by raw array index (`i + 1`), not the tie group - two members tied for 1st and literally splitting the same R4,500 showed as `#1` gold and `#2` silver, a ranking difference that didn't exist and directly contradicted the "(tied)" label sitting next to their matching prize amounts.
- **Status**: Fixed (commit `c5d69b8`) - rank is now computed with standard tie-aware ("1224") ranking, and medal color follows both that rank and whether the row actually has a prize (so a 0-room member never gets a trophy from sitting in row 2 of a mostly-empty leaderboard).

#### [Competitions] Rules guide claimed "only one submission per day", contradicting the actual/intended behavior
- **Severity**: Cosmetic
- **Where**: `src/components/CompetitionRulesModal.jsx` (Daily limit section) vs. `submit_daily_room_log()` and the in-app form copy ("You can still update the count below until it's reviewed")
- **What's wrong**: Doc-vs-code mismatch per the QA methodology's own call-out. The rules guide is the one that was wrong - the system deliberately allows updating a Pending submission any number of times before review.
- **Status**: Fixed (commit `238d51a`).

#### [Competitions] RSVP display name was trusted from the client, not resolved server-side
- **Severity**: Medium
- **Where**: `supabase/053_competition_opt_out.sql` - `rsvp_for_competition()` (this file owns the live definition, per its own header note superseding `015`/`016`)
- **What's wrong**: `p_member_name` was written straight onto `competition_standings.member_name` - a public leaderboard tied to real prize money (up to R6,000/quarter). The UI always sends the caller's real Google name, but the RPC itself had no check - a signed-in member calling it directly (browser console, or any API client) could pass any string and impersonate another real member, or post anything, in front of the whole community. The rest of this app already avoids exactly this pattern elsewhere (`challenge_to_room_race()` in `063_room_races.sql`, `notify_admin_of_roadmap_completion()` in `061_admin_notifications.sql` both resolve the caller's name server-side from `member_profiles` instead of trusting a parameter) - this RPC was the one place in the Competitions surface still doing it client-supplied.
- **Status**: Fixed (commit `3f3846f`) - now resolves `full_name` from `member_profiles` server-side (same idiom as the two functions above), with the same `COALESCE`-to-email fallback for a member with no `full_name` set. `p_member_name` stays in the signature so the frontend call needed no change. Applied to the live DB.

#### [Competitions] No reset/archival mechanism between quarterly competitions
- **Severity**: Medium-High (not urgent today, but time-sensitive)
- **Where**: `supabase/015_competition_standings.sql` / `016_days_logged.sql` / `053_competition_opt_out.sql` (schema), `src/views/Member/MemberPortal.jsx` - the hardcoded `currentCompetition` object (~line 2347: title "Q3 2026 Community CTF Sprint", `startDate: '2026-08-31'`, `endDate: '2026-10-23'`)
- **What's wrong**: `competition_standings`/`daily_room_logs` have no notion of a competition period at all - no `competition_id`/season column, no admin "start a new competition" action, no archival step. `currentCompetition` is a plain hardcoded literal in the member portal source with no admin UI backing it - changing quarters means editing this object in a code commit. Room logging is correctly blocked outside the active window (`competitionStatus === 'Upcoming'`/`'Ended'`), but the *standings themselves* never reset - `rooms_completed`/`days_logged` just keep accumulating forever. Searched the whole codebase (SQL, admin dashboard, data layer) for any reset/archive/season concept and found none. Today is 2026-09-11, squarely inside the current Q3 window (ends 2026-10-23) - about six weeks out. Whoever updates `currentCompetition` for Q4 will find the leaderboard still showing every member's all-time cumulative total from Q3 (and beyond), not a fresh Q4 count, silently changing who "wins" each quarter's prize unless someone manually zeroes the table first.
- **Is this intentional?** Almost certainly an oversight rather than a decision, since this appears to be the very first competition run through this system (Q3 2026 is active right now) - the "what happens next quarter" problem hasn't been hit in practice yet. Founder asked to implement it directly (2026-09-11, later same day).
- **Status**: Fixed (commit `1875241`) - new `competitions` table (`070_competition_seasons.sql`) is the real source of truth for the currently-running competition, seeded with the actual live Q3 season so nothing visibly changed on deploy. `start_new_competition()` archives a full JSON snapshot of `competition_standings` onto the row that just ended, clears the live table for a fresh quarter, and inserts the new one as current - one transaction. Deliberately NOT a `competition_id` column threaded through every already-hardened function from the passes above (`rsvp_for_competition`, `submit_daily_room_log`, `review_daily_room_log`, `correct_room_log_review`) - avoidable risk of reintroducing a bug in code already fixed twice today. New admin UI on the Room Logs tab: current-competition summary, a guarded "Start New Competition" form (confirm dialog spells out that it archives + resets), and an expandable Past Competitions list per archived season. `start_new_competition()` was applied live but deliberately NOT invoked against the real Q3 competition - firing a real reset is the founder's call to make when Q3 actually ends, not something to trigger while building the tool.

#### [Competitions] No admin path to correct a mistaken room-log review decision
- **Severity**: Low-Medium
- **Where**: `src/views/Admin/AdminDashboard.jsx` - the "Reviewed" room-log list (~line 4044-4069) renders read-only, no Approve/Reject once a log leaves Pending; `review_daily_room_log()` has no re-review path either (see the concurrency fix above - it now actively *blocks* re-reviewing on purpose).
- **What's wrong**: If an admin/CM approves or rejects the wrong log, or approves with a typo'd rejection note, there is currently no way to undo or correct it - not through the UI, and now not through the RPC either (my concurrency fix makes this explicitly impossible, closing off what little bypass existed). This mirrors the same shape as the "no path to approve a Project that was never submitted" finding from the previous pass: could be a deliberate "review is final, no do-overs" stance (keeps the anti-cheating trail simple and disputes get resolved by an admin manually editing `competition_standings` directly if it ever comes up), or could be an oversight nobody's hit yet.
- **Is this intentional?** Unclear - logged rather than decided. Founder asked to fix it (2026-09-11, later same day) rather than leave it open.
- **Status**: Fixed (commit `8ee3c19`) - new `correct_room_log_review()` RPC, callable only on an already-reviewed log (Pending stays exclusively `review_daily_room_log()`'s territory), safely re-derives the standings credit (undoes the old one if it was Approved, re-applies if the new decision is too) rather than re-running the increment, and locks the row for the same double-adjustment reason the original concurrency fix does. Surfaced as a "Correct" control next to each reviewed log, behind a confirm dialog since it can remove credit already given.

**Next up**: Head-to-Head Duels (`062_quiz_duels.sql`), Room Races (`063_room_races.sql`), and Live Buzzer Trivia (`066_live_trivia.sql`) are the natural continuation of Competitions - same tab, same admin section, unread this pass. Note in particular that `challenge_to_room_race()` trusts `p_opponent_name` from the client for the *other* player's name (only the caller's own name is resolved server-side) - worth a look given the spoofing issue just fixed in `rsvp_for_competition()` was the same shape. Otherwise, **My Subscription & Upgrades** (PayFast) and the **RLS/permission-scope audit** across `067_permission_scopes.sql` remain good, unclaimed picks.

---

Claimed (third pass, same day): **Head-to-Head Duels, Room Races, and Live Buzzer Trivia** (`062_quiz_duels.sql`, `063_room_races.sql`, `066_live_trivia.sql`), finishing out the Competitions area per the prior pass's own suggestion - specifically checking whether `challenge_to_room_race()` had the same client-trusted-opponent-name bug just fixed in `rsvp_for_competition()`. It did, and so did its sibling `challenge_to_duel()`, which the prior pass hadn't looked at yet. Also read `067_permission_scopes.sql`'s slice covering these three tables (Community Manager widening for Room Races and Trivia session hosting), `src/lib/quizDuelData.js`, `src/lib/roomRaceData.js`, `src/lib/triviaData.js`, and the Competitions tab + Room Logs admin tab render in both `MemberPortal.jsx` and `AdminDashboard.jsx`. Competitions is now fully covered - checking the box.

#### [Competitions] Quiz Duel and Room Race challenges trusted the opponent's name from the client
- **Severity**: Medium
- **Where**: `supabase/062_quiz_duels.sql` - `challenge_to_duel()`; `supabase/063_room_races.sql` - `challenge_to_room_race()`
- **What's wrong**: Both functions wrote `p_opponent_name` straight onto `member_b_name` with no server-side check - the exact same shape as the `rsvp_for_competition()` bug fixed last pass. The honest UI always sends the real name from the members directory (`MemberPortal.jsx`'s `selectedDirectoryMember.fullName`), but a signed-in member calling either RPC directly (browser console, or any API client) could pass any string for `p_opponent_name` and have it displayed next to a real opponent's real email - to the opponent themselves (in their own Duels/Races list), and to whichever admin/Community Manager later reviews a Room Race submission. For Quiz Duel specifically, a spoofed name could reach the public `community_wins` feed via `announce_duel_win()` if the challenger (who controls `p_opponent_name`) goes on to win - "Won a head-to-head Quiz Duel against `<attacker-supplied text>`" shown to the whole community, tied to a real opponent's real email. `053_competition_opt_out.sql`'s own comment on the `rsvp_for_competition()` fix cited `challenge_to_room_race()` as already resolving "the caller's own real name" server-side, which was true - but only for the caller's own name (`member_a_name`), not the opponent's (`member_b_name`), which is the half that was still broken.
- **Status**: Fixed (commit `6423777`) - both functions now resolve the opponent's name server-side from `member_profiles` by their (already-validated) email, same `COALESCE`-to-email fallback used throughout these files. `p_opponent_name` stays in both signatures so neither frontend call needed to change. Applied to the live DB via `npm run db:apply`; verified via `pg_get_functiondef` that both live definitions now resolve the opponent name.

#### [Competitions] Live Buzzer Trivia read clean - no equivalent name-spoofing bug
- **Severity**: N/A (negative finding, noted for the record)
- **Where**: `supabase/066_live_trivia.sql` - `join_trivia_session()`, `buzz_in_trivia()`
- **What's wrong**: Nothing - checked specifically because it's the third sibling in the same "head-to-head competitions roadmap" family as the two bugs above. `join_trivia_session()` resolves `member_name` server-side from `member_profiles` (never client-supplied), and `buzz_in_trivia()` reads the buzzer's name back off `trivia_participants` rather than accepting one as a parameter at all. The row-lock fairness mechanism (`SELECT ... FOR UPDATE` in `buzz_in_trivia()`) also held up under a skeptical read - it does correctly serialize concurrent buzz-ins per the header comment's claim.
- **Status**: N/A - no bug found, no fix needed.

Everything else read in this pass held up: the tie-vs-void distinction between `submit_duel_answer()`'s inline completion (a genuine tie when both finish is `status='Completed', winner_email=NULL`, correctly rendered by `MemberPortal.jsx` as "Tied — no winner") versus `resolve_expired_duels()`'s forfeit sweep (a tie there, including 0-0, is `status='Void'`) looked at first like it might be two copies of tie-handling that could drift, but they're deliberately different situations (finished-and-tied vs. nobody-finished) and the UI already branches on both correctly. `approve_room_race_submission()`'s concurrency shape (two submissions approved in quick succession) is explicitly documented and intentional ("whoever gets approved first wins") and, unlike the room-log double-credit bug from last pass, has no numeric increment to double-apply - a second concurrent approval just silently no-ops after the row leaves `'Active'`, with no visible inconsistency since Completed races drop out of the admin queue and the member UI stops rendering per-player submission state once a race is Completed. `067_permission_scopes.sql`'s Community Manager widening for Room Races and Trivia session hosting matches the UI's assumptions (no admin-only affordance left reachable by a CM that shouldn't be, no CM-intended affordance still gated to admin-only).

**Next up**: **My Subscription & Upgrades** (PayFast checkout money-handling) and the **RLS/permission-scope audit** across `067_permission_scopes.sql` remain the two best unclaimed picks - both fit the "complex logic / money-handling" bar this project prioritizes, and neither has been touched by any pass yet. Do NOT build a quarterly competition reset/archival mechanism if picking this area back up later (see the "No reset/archival mechanism" finding above) - it's logged and waiting on a founder decision, not something to build unilaterally.

---

Claimed (fourth pass, same day): **My Subscription & Upgrades** (member) + **Payments & Subscriptions** and **Merch Orders** (admin) - the money-handling area flagged as highest-value by every prior pass. Read `033_payfast_transactions.sql`, `046_member_billing_summary.sql`, `060_merch_orders.sql`, `004_member_access_control.sql` (`grant_member_portal_access`), `supabase/functions/payfast-checkout/index.ts`, `supabase/functions/payfast-webhook/index.ts` (read-only per the fix policy's webhook boundary), `src/lib/payfast.js`, `src/lib/payfastPaymentsData.js`, `src/lib/billingData.js`, `src/lib/merchStoreData.js`, `src/views/Login.jsx`, and the My Subscription & Upgrades / Payments & Subscriptions / Merch Orders sections of `MemberPortal.jsx` and `AdminDashboard.jsx`. This is the most severe pair of findings logged so far - one fixed, one deliberately not.

#### [My Subscription & Upgrades] PayFast checkout amount is entirely client-controlled - no server-side price validation tied to the plan being purchased
- **Severity**: Critical
- **Where**: `supabase/functions/payfast-checkout/index.ts` (the whole function - `amount` is taken straight from the request body with only `amount > 0` checked); `src/views/Member/MemberPortal.jsx` - `handlePayfastPay(planName, amount)` (~line 2609), called as `handlePayfastPay(tier.name, tier.amount)` (~line 6847)
- **What's wrong**: The signed, PayFast-bound checkout `amount` comes directly from whatever the caller's JSON body says, with zero cross-check against a real plan-price allowlist anywhere server-side. The honest UI always sends `tier.amount` from the hardcoded `ALL_TIERS` list, but `payfast-checkout` is a plain authenticated Edge Function - any signed-in Supabase user (which, per `Login.jsx`'s `handleGoogleLogin`, is anyone with a Google account; `supabase.auth.signInWithOAuth` runs with no pre-check, membership is only enforced afterward by the portal UI reading `is_member_allowed()`) can call `supabase.functions.invoke('payfast-checkout', { body: { amount: 1, subscriptionType: 1, itemName: 'Hacking Hub - Monthly Operative', ... } })` directly from a browser console and get back a validly-signed PayFast checkout URL for R1. After a genuine R1 PayFast payment completes, `payfast-webhook` sees `payment_status === 'COMPLETE'` and unconditionally calls `grant_member_portal_access()` (service-role, so it skips that RPC's own admin-only guard, which only gates `auth.role() = 'authenticated'` callers) - no amount check anywhere in that path. Net effect: anyone with a Google account and a PayFast account can buy full, permanent member portal access for a fraction of a rand, or an existing member can "renew" the same way indefinitely. This is a direct, easily-reproducible revenue-theft and access-control bug, not a theoretical one - it requires no privileged access, just devtools.
- **Why not fixed directly**: A real fix means giving `payfast-checkout/index.ts` its own server-side plan-price allowlist (keyed by `subscriptionType`/`itemName`) and rejecting/clamping any request that doesn't match, which means editing and redeploying a live PayFast-facing Edge Function - exactly the "PayFast/payment logic" the fix policy says never to touch unilaterally, and there's no PayFast sandbox available in this environment to verify a redeploy doesn't break checkout for real paying members (a bad edit here risks breaking revenue collection entirely, which is worse than leaving the current gap open a bit longer). The merch-side equivalent of this bug (see below) was fixable with a pure SQL trigger and no Edge Function change - this one isn't, because a subscription checkout has no database row created before the Edge Function call for a trigger to validate against.
- **Recommended fix shape** (for whoever picks this up with real deploy/test access): a `PLAN_PRICES` map in `payfast-checkout/index.ts` keyed by `subscriptionType`/`itemName`, ignoring the client's `amount` for known plans (Basic Access/Monthly Operative/Permanent Access) and only trusting a client amount for genuinely ad-hoc cases (Elite Operative apply-only, EFT top-ups aren't affected - those are admin-only); and/or have the webhook decline to call `grant_member_portal_access()` when `amount_gross` falls below a sane minimum for the claimed plan.
- **Status**: Fixed (commit `651eb5d`) - `payfast-checkout` now rejects any non-merch checkout whose amount isn't one of the three real membership prices (R200/R600/R1000), deployed live. The legitimate flow for all three real tiers is byte-for-byte unchanged, so the blast radius on a real purchase is effectively zero even without a sandbox to test against - but a real end-to-end purchase is still worth doing when possible to be fully sure.

#### [Merch Orders] Order total_amount was never validated against real product pricing - a member could buy a R600 hoodie for R1
- **Severity**: Critical
- **Where**: `supabase/060_merch_orders.sql` - the `merch_orders` INSERT policy (only enforced `total_amount > 0`, ownership, and default status); `supabase/functions/payfast-webhook/index.ts` merch branch (marks an order 'Paid' by comparing real ITN `amount_gross` against exactly this client-supplied `total_amount`)
- **What's wrong**: Same shape as the subscription bug above, but on a table this app already creates a database row for before checkout, which made it fixable in pure SQL. `total_amount` arrived from `createMyMerchOrder()`'s client-computed `merchCartTotal` with nothing tying it to `items` or `MERCH_CATALOG` pricing. A member could insert an order for a real Hoodie (`items` correctly saying so) with `total_amount` hand-set to 1, pay R1, and have the webhook's own amount-match check (`abs(amount_gross - total_amount) < 0.01`) pass cleanly - marked 'Paid' for what should've been R600.
- **Status**: Fixed (commit `1fc3261`) - a new `validate_merch_order_total()` trigger (`BEFORE INSERT ON merch_orders`) recomputes the true total from `items` against a server-side mirror of `MERCH_CATALOG` and overwrites whatever `total_amount` the client sent, regardless of it. This makes the webhook's existing amount check trustworthy again without touching the webhook or checkout Edge Function at all. Applied to the live DB via `npm run db:apply`; verified with a synthetic test insert (Hoodie, `total_amount` lied to `1`) that came back corrected to `600`, then deleted.

#### [My Subscription & Upgrades] Current Active Clearance card silently showed the wrong plan for every PayFast-paid member
- **Severity**: Medium
- **Where**: `src/views/Member/MemberPortal.jsx` - `matchedTier`/`currentPlanRank` (~line 2765, pre-fix) vs. `handlePayfastPay`'s `itemName: \`Hacking Hub - ${planName}\`` (~line 2615)
- **What's wrong**: `payfast_transactions.plan` stores PayFast's `item_name` verbatim, and every self-service checkout sends that as `"Hacking Hub - <tier name>"` (e.g. `"Hacking Hub - Monthly Operative"`), but the plan-matching logic compared it against the bare `ALL_TIERS` name (`"Monthly Operative"`) - which never matches. Every member who paid via the (now-working) PayFast checkout silently fell back to `currentPlanRank = 1` (Basic Access) regardless of what they'd actually paid for, so their own subscription page showed the wrong current plan and offered a full set of "upgrade" tiers including ones below what they already had. Admin-recorded EFT payments were unaffected - `RecordEftPaymentModal` stores the bare `MEMBERSHIP_TIERS` name with no prefix, so that path matched fine, which is presumably why this wasn't caught by casual admin-side testing.
- **Status**: Fixed (commit `1fc3261`) - strips the known `"Hacking Hub - "` prefix before matching, handling both PayFast- and EFT-sourced plan values. Pure frontend change, no Edge Function or schema involved.

#### [Merch Orders] No admin path to resolve a 'Needs Review' order once it exists
- **Severity**: Medium
- **Where**: `src/views/Admin/AdminDashboard.jsx` - the Merch Orders tab (~line 4791-4801) only renders "Mark Fulfilled"/"Cancel" buttons when `order.status === 'Paid'`; nothing renders for `'Needs Review'` beyond a static warning message
- **What's wrong**: `'Needs Review'` is a real, expected state (the webhook parks an order there on an amount mismatch rather than guessing), but once an order lands there, the shipped admin UI gives zero way to move it forward - not to 'Paid' after manual reconciliation with the real bank/PayFast record, not to 'Cancelled' either. The code comment above the state (`"Read-only from the admin's perspective except fulfillment status... never from here"`) suggests this was a deliberate boundary for the *normal* Paid→Fulfilled/Cancelled flow, but 'Needs Review' existing as a status at all implies someone is supposed to be able to act on it - today that only works via a direct SQL write outside the app (the admin `FOR ALL` RLS policy allows it, the UI just doesn't expose it). Same shape as the "no admin path to approve a Project without proof" / "no admin path to correct a room-log review" gaps from earlier passes, both of which were fixed at the founder's request once flagged.
- **Is this intentional?** Unclear - logged rather than decided, per the fix policy. My guess: an oversight (nobody's hit a real amount-mismatched merch order yet, so the dead-end was never noticed) rather than a deliberate "review is final" stance, since there's no equivalent anti-cheating reason to keep it locked the way room-log re-review had.
- **Status**: Fixed (commit `ac6776a`, fifth pass, 2026-09-11) - same shape as the two admin-override gaps fixed earlier the same day (Project approval without proof, room-log review correction), so fixed directly per that established precedent rather than left open. Added "Mark Paid (Reconciled)" and "Cancel" controls for Needs Review orders (RLS already allowed this via the existing admin `FOR ALL` policy - purely a missing UI affordance, no schema change needed), both behind a confirm dialog since marking Paid is an admin asserting they've manually checked the real PayFast/bank record and overriding the automatic amount-match check that parked the order there. `updateMerchOrderStatus()` now also sets `paid_at` on a manual Paid resolution, matching the webhook's own write.

**Next up**: The **RLS/permission-scope audit** across `067_permission_scopes.sql` remains the one major cross-cutting area no pass has touched yet - it's explicitly flagged by every prior pass as a top pick and is now the last "complex logic / money-handling-adjacent" area left unclaimed. If picked up: do NOT attempt the quarterly competition reset (needs founder design input, logged above) and do NOT attempt a live redeploy of `payfast-checkout`/`payfast-webhook` to fix the Critical subscription-amount-tampering bug logged just above unless real PayFast sandbox/test access is available - that fix needs to be verified against a real gateway, not guessed at blind.
